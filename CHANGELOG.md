# MacroFlow Studio 更新历史

## v1.2.2（快捷键脚本执行修复）

### 修复

- **修复**：快捷键转向脚本（如 J 左转 90°）在游戏执行时无效——录制时游戏在前台且锁定光标，转向位移从屏幕中心起算；回放时若游戏不在前台（如 MacroFlow 窗口挡在游戏前）或光标停在屏幕边缘（上次转向停下的位置），同样的 ΔX/ΔY 会被桌面边界截短或完全被游戏忽略，坦克不转。现在：
  - 快捷键脚本含「转向」或相对移动动作时，执行前自动把绑定的游戏窗口带到前台（游戏重新锁定光标后转向恢复正常）
  - 每次转向动作前自动把光标移回目标窗口中心再发送位移，完整重现录制时的转向量；锁中心类游戏不受影响（游戏自身会把光标拉回中心）
  - 未绑定目标窗口时在日志中给出明确提示（“未找到绑定的目标窗口，相对转向可能无法生效”）
- **注**：游戏在菜单/大厅等非战斗界面本身就不响应鼠标转向（未锁定光标），此状态与执行环境无关，属游戏自身行为
- **修复**：录制时按快捷键，快捷键脚本回放产生的鼠标移动会被录成普通「鼠标移动」动作（绝对坐标），与快捷键脚本里的「转向」动作不一致，且该移动在游戏中执行时不生效。现在：
  - 快捷键脚本回放注入的相对位移在录制时录成**「转向」动作**（ΔX/ΔY，50 ms 内连续位移合并为一条），与快捷键脚本内容一致，游戏中执行时走转向的前置/居中逻辑即可生效
  - 回放注入的绝对坐标移动不再录制（避免录进游戏中无效的“移动”动作；点击等动作自带坐标，不依赖移动）
  - 所有录制模式（桌面/自动/游戏相对）下都识别注入位移，物理鼠标移动的录制行为不变
- **修复**：录制时按快捷键注入的「转向」动作会丢失时间与顺序信息——注入位移只在下次注入或停止录制时才落成动作，导致转向的延时被记到下一次快捷键/停止的时间点（如 16 秒时按下的转向被记成 30 多秒），且转向会排到它之后发生的实体点击/按键之后。现在：
  - 注入的转向动作记录快捷键**实际按下时刻**，延时从按下时刻起算，不再推迟到下一次快捷键或停止时
  - 注入转向后一旦出现实体输入（按键/点击/滚轮/鼠标移动），立即把转向落成动作，保持真实动作顺序
  - 多次快捷键注入各自保留自己的按下时刻与延时，不再串扰合并到错误时间点
- **修复**：编辑转向动作时 ΔX/ΔY 或「执行前延时」会互相丢失——动作编辑对话框保存时重建动作字典，只保留对话框里展示的字段，导致未展示字段（执行前延时 delay_ms、执行后延时、转向步数等）被清零；且转向编辑对话框原本没有「执行前延时」输入框，无法直接修改。现在：
  - 转向编辑对话框新增「执行前延时」输入框，可直接修改转向前的等待时间
  - 转向/鼠标移动/点击/连续点击/键盘/文本/跳转动作编辑均基于原动作更新，保留所有未展示字段（执行后延时等不再丢失）

## v1.2.1（快捷键脚本）

### 快捷键调用脚本

- **新增**：侧栏新增「快捷键脚本」区（设置… 按钮）——把**「scripts/方向」目录中的脚本**绑定到单个按键（字母、数字、F1-F12、方向键、空格、回车等），在录制和执行脚本的过程中按下快捷键立即执行绑定的脚本；例如游戏中按 J 执行「转向左 90°」脚本；执行脚本只能从「scripts/方向」目录选择（下拉与绑定列表只显示脚本名，不显示目录）
- 录制时按快捷键：快捷键本身不会录进当前脚本，脚本回放的按键与鼠标输入会被录进去——录制大脚本时可用快捷键把预录的小动作（转向、换弹、喝药等）一键“录进”当前脚本
- 执行时按快捷键：绑定的脚本在独立播放器中并行执行（不打断当前脚本或工作流），F12 紧急停止同时终止所有正在执行的快捷键脚本
- 绑定列表随 `app_settings.json` 保存，重启自动恢复；同一快捷键同时只允许一个绑定，F8/F9/F12（系统功能键）与 Shift/Ctrl/Alt/Win 等纯修饰键不可绑定
- 快捷键脚本按**纯动作**方式执行：不注册全局守卫、不切换专注模式、不操作主执行界面；只支持单键触发（不含组合键）
- 强制专注模式下快捷键同样生效：按键由输入守卫钩子直接识别并触发（被拦截的按键不会送达游戏）

### 新增动作：鼠标转向

- **新增**：「↺ 转向」动作——鼠标相对移动 **ΔX/ΔY**，不按键，用于游戏内视角转向（如按 J 执行「转向左 90°」脚本）
- 只填两个数：ΔX（正值向右）与 ΔY（正值向下）；执行时一次相对移动（1 步约 10 ms）完成转向，无坐标选取、无按键
- 动作摘要、编辑对话框（双击编辑）、模块代码段摘要同步支持该动作
- **新增**脚本类别「方向」：保存脚本时选「方向」类别即存入 `scripts/方向` 目录，与关卡封装并列；目录设置对话框同步增加「方向脚本目录」一项

### 修复

- 修复：快捷键脚本设置对话框（绑定列表 / 单个绑定编辑）在打包版按真实 DPI 渲染时显示不全——固定窗口尺寸装不下高 DPI 下的实际内容，底部按钮行被挤出窗口、脚本列被裁切；现按内容实际需求自动调整窗口尺寸并居中（与模块管理等其他对话框一致），脚本列随窗口拉伸

### 录制与保存

- **调整**：录制不再弹任何确认框——当前脚本有未保存的修改时按 F8 重新录制直接清空动作列表开始（已保存的旧文件不受影响，保存新录制时提示是否覆盖）；不再被"请先保存"拦住
- **调整**：重新录制不再自动分离成"原名_新录制"新文件，而是直接覆盖当前文档；保存时按覆盖流程处理
- **新增**：保存已存在的脚本文件时提示"是否覆盖"，确认后才写盘；覆盖前旧版本自动归档到 `backups/overwritten/`（保留完整目录结构与时间戳，同一脚本的每次覆盖都留下独立历史文件，可随时找回被覆盖的版本）

## v1.2.0（源码结构重构与目录整理）

### 工程结构

- **源码包化**：全部源码从根目录平铺移入 `src/macroflow/` 包，按功能域分文件夹——`core/`（models/storage/ocr/image_match/alerts 数据与基础能力）、`input/`（wininput/input_guard/recorder/rawinput 键鼠输入层）、`execution/`（player 执行引擎）、`ui/`（app/dialogs/detect_overlay 界面层）、`diag/`（诊断脚本）；测试移入 `tests/`
- **入口与导入路径统一**：源码运行 `python -m macroflow.ui.app`（run.bat 已更新），支持脚本直跑（开机自启动命令自动带 `src` 路径引导）；模块间导入全部改为 `macroflow.*` 包路径；`MacroFlowStudio.spec` 入口与 `verify_build.py` 模块检查同步更新
- **数据路径不变**：`storage.app_dir()` 源码模式返回项目根（向上四级定位），运行时数据（scripts/workflows/images/backups/app_settings.json）仍生成在项目根目录，与打包版"exe 同目录"语义一致，源码与数据互不污染
- **目录清理**：删除空目录 `tmpd5blwd69/`、解包测试产物 `_meitest/`（12671 个文件）、`__pycache__/`、测试残留 `_ocr_test.png`/`_modlens_test.json`/`crash.log`；旧发布包 `MacroFlowStudio_latest_win64.zip` 与模型包 `cls.tar`/`rec.tar` 归档到 `release-notes/`
- 使用 `git mv` 移动文件，完整保留提交历史；`python tests\test_core.py` 全量测试通过（751 个）

### 允许前台窗口

- **移除**：「允许前台窗口」白名单功能整体删除（v1.1.4 引入）——侧栏列表与按钮、「关闭软件」进程选择器联动、设置持久化、执行期间前台守卫的白名单逻辑全部移除；执行期间前台判定恢复为按**主绑定窗口**进行，前台守卫仍保留（含等待期间每 500ms 检查、截图轮后恢复、点击后恢复），外部窗口抢占焦点时自动抢回并在日志记录抢占者（标题+进程名，限频 10 秒），便于排查失焦来源
- 注：v1.1.4 中该功能的相关历史条目保留仅供追溯，当前版本已不再提供

## v1.1.4（允许前台窗口白名单）

### 执行与前台

- 新增：侧栏「目标窗口」区新增「允许前台窗口」列表——把游戏大厅、游戏本体等合法窗口加入**可排序列表**（▲ 上移提高优先级 / ▼ 下移降低 / ✕ 删除选中 / 双击删除，越靠上优先级越高）。执行期间前台应保持列表中**第一个当前打开的窗口**：其他程序弹窗、误点，甚至列表中优先级较低的窗口抢到前台，都会在几百毫秒内自动抢回；列表为空时按主绑定窗口判定（原行为）
- 新增：白名单随 `app_settings.json` 保存（按 标题/类名/进程路径 签名存储，不依赖易失效的窗口句柄），重启软件后自动恢复，无需重新添加；进程路径匹配兜底，游戏更新重启后窗口也能找回
- 调整：长延时与识图/OCR 轮询等待期间每 500ms 检查一次期望窗口前台（原来只在输入动作前后检查），被抢焦点后自动抢回
- 调整：截图轮后的前台恢复与播放开始的前台判定同步使用白名单（优先级语义一致）
- 重构：窗口签名解析逻辑（`_execute_activate_window` 的 title/类名/进程路径匹配）提取为 `wininput.resolve_window_signature`，播放器与主窗口共用一份

## v1.1.3（工作流复制与改名）

### 工作流

- 新增：工作流页名称框旁新增「✏️ 修改名称」按钮——弹窗输入新名称后立即保存，文件同步改名（沿用“绝不覆盖”去重规则，重名时自动追加序号，旧文件删除）
- 新增：「⧉ 复制为新工作流」按钮——把当前工作流完整复制为一个独立新工作流（新文件、步骤 ID 全部重新分配，不继承原计划的定时开始时间），保存后直接打开副本，可在副本上继续修改，无需重新插入步骤

## v1.1.2（工作流插入位置修复）

### 工作流

- 修复：「添加当前脚本」「选择已有脚本」「添加模块」此前一律追加到工作流末尾，无视「插入位置 ▲ 上方 / ▼ 下方」选项和当前选中的行——现在有选中行时按插入位置设置插到选中行上/下方，未选中行时才追加到末尾（保持“添加”语义）；「插入脚本」「插入模块」行为不变
- 调整：多选模块插入时按选择顺序连续插入；插入完成后选中新插入的行，方便连续操作

## v1.1.1（执行启动修复）

### 执行启动

- 修复：启动后第一次执行（F9 / 工作流 / 单独测试）会无条件等待后台 OCR 引擎预加载（首次导入可能数十秒且不可中断），期间状态一直停在“正在执行脚本…”、没有任何动作执行、强制专注模式也尚未生效（鼠标还能动）——现改为：只有脚本动作树实际包含文字识别（`text_ocr` 动作、文字守卫 `recognize=text`、引用模块代码段、备用文字模块、递归展开的 `script_ref` 引用脚本）时才等待 OCR；纯键鼠/模板匹配脚本跳过等待立即开始。判断采用保守策略：引用脚本解析失败时按需要 OCR 处理，宁可在播放开始前多等，也不把不可中断的导入留到播放中途
- 调整：强制专注模式（输入法切换 + 系统输入锁）在等待 OCR 之前即生效，按下 F9 / 启动工作流后输入立即锁定，不存在“提示正在执行却还能动鼠标”的窗口期；等待期间 F12 仍可中止
- 调整：需要等待 OCR 引擎时，执行进度明确显示“正在加载 OCR 引擎（首次可能数十秒）· 按 F12 中止”并写入日志，不再只是笼统的“正在准备”

## v1.1.0（守卫引擎重构）

### 全局检测架构：线程抢占 → 守卫引擎

- 全局检测（工作流全局模块 / 脚本全局模块 / 独立全局脚本）不再为每个模块启动后台检测线程，改为**守卫数据 + 播放器内联评估**：播放器在动作边界与长等待期间统一评估全部守卫，一次截图喂全部图片守卫
- 触发处理段（模块脚本 / 代码段 / 跳转 / 二次识别点击 / 备用模块点击）在播放器线程**内联执行**，不再停止播放器、记录断点快照、销毁并重启全部监控
- 消除旧模型的竞态根源：跨线程中断、断点恢复状态机（resume_index / pending_restart / rearm 锁等十余个状态变量）、监控重启风暴与随之而来的截图突发 / 游戏失焦
- 守卫生命周期 = 一次执行；断点恢复、特殊模块「重新执行工作流」不再重建监控
- 检测间隔语义变为最小检查间隔（节流）：文字守卫下限 500ms，图片守卫按配置间隔；未到点不截图
- 触发后守卫进入“等待目标消失”重新武装；超时处理段 / 等待文字消失 / 备用模块（含节流点击）语义保持不变

### 失焦修复

- 全屏截图后立即校验绑定窗口前台，失焦时自动恢复（原来只覆盖工作流全局扫描路径）
- 全局检测点击坐标越界时收敛进目标窗口再点击；点击后立即确认目标窗口仍在前台，失焦窗口自动恢复前置
- `activate_window` 不再无条件 SetFocus：窗口已在前台时保持焦点不动，避免从游戏内部渲染表面（Flash/CEF 画布）夺走键盘焦点

### 工作流

- 「重新执行工作流」默认跳转行从“模块设置文件”迁移为**工作流统一设置**（随工作流文件保存）：工作流页面新增“重新执行默认跳转行”下拉框，动作级跳转行优先于统一默认
- 修复：侧栏取消勾选「启用执行前置窗口」后，工作流步骤脚本自身保存的前置窗口仍会被激活的问题——该开关现为本次执行的统一总开关，未勾选时工作流不会执行任何前置窗口（即使步骤脚本自己勾选并保存过）
- 修复：特殊模块「重新执行工作流」触发时向 `run_workflow` 传入了不存在的 `suppress_start_sound` 参数，导致 TypeError、重启直接失败——`run_workflow` 现支持该参数，重启时不再重复播放开始提示音
- 修复：F12 紧急停止无法取消已排定的「重新执行工作流」——交接窗口内残留轮询会在 worker 退出后把工作流重新拉起（且次数快照已被清空）
- 修复：工作流正常结束后全局模块守卫残留，会在之后的单独脚本执行中继续触发——守卫生命周期 = 一次执行，worker 收尾统一清空
- 修复：录制中可启动工作流（与单脚本/单独执行入口的互斥处理不一致），录制小窗被销毁且注入的输入被录进去——`run_workflow` 入口与其余入口一致地先停止录制
- 修复：纯全局模块工作流（无脚本步骤）注册守卫后立即“执行完成”、检测永不生效——现进入驻留检测模式持续评估守卫直到 F12 停止（恢复 v1.0 常驻监控行为）
- 修复：步骤间隙等待中守卫处理段要求结束/推进/跳转时，`_guard_wait` 把 False 当终止、静默结束整个工作流——无脚本上下文时跳过剩余等待并继续工作流
- 修复：工作流启动延时亚秒值被 round 截断（500ms→0s、2500ms→2s）——整秒存储改为向上取整，延时只多不少
- 调整：执行前置窗口未打开时一律跳过前置激活并继续执行（工作流启动、步骤脚本、单独执行、F9 当前脚本一致），不再提示“请重新选择或取消勾选”，也不再阻止脚本执行；播放开始时前置窗口已关闭同样跳过而非报错

### 回放

- 修复：滚轮动作播放必然 TypeError（`send_scroll(dy)` 与双参数签名不匹配，横向滚轮也永远发不出）——现按 `send_scroll(dx, dy)` 正确转发
- 修复：F12 在“按下-抬起”窗口内停止会物理卡住按键/鼠标按钮（key_press / 点击 / 连续点击 / 模块点击）——按下即登记 held 状态，停止时由 `_release_all` 保证抬起
- 修复：嵌套执行段（引用脚本 / 模块代码段）内守卫跳转被内层动作序列错误消费——按设计统一交给最外层（depth==0）解析
- 修复：重复间隔等待期间守卫命中携带跳转/结束/推进语义时异常逃出 `play()` 导致“工作流执行失败”——跳转应用到下一次重复的起始行，结束/推进按工作流语义收尾
- 修复：`key_press` 缺少 vk=0 校验（与 `key` 分支不一致），会发送非法虚拟键事件
- **模板匹配分辨率适配**：识图、二次识别、备用模板与全局检测的模板匹配此前只缩放坐标不缩放模板——执行机截图尺寸与录制机不同（多显示器虚拟屏幕、分辨率/DPI 差异）时目标像素大小与模板不一致，匹配度下降甚至失败。现按“执行机屏幕宽度 / 录制机屏幕宽度”等比缩放模板后再匹配（尺寸相同零开销），匹配结果坐标仍为当前屏幕坐标系
- **F12 紧急停止可靠性**：文本发送改为逐字符发送且间隔等待检查停止信号（长文本期间 F12 不再延迟数秒）；热键回调异常不再杀死 pynput 监听器（否则 F8/F9/F12 全部失效且无提示），监听器启动失败会写入日志；紧急停止 3 秒后工作线程仍未退出时，把线程堆栈写入日志便于定位卡点（OCR/截图等不可中断调用）
- **OCR 引擎预加载**：首次导入 paddle 全家可能耗时数十秒且不可中断（此前第一次文字识别会在播放中途卡住、F12 无效，日志可见卡在 `_get_engine` 的 import 链）——启动后立即后台预加载（并发初始化加锁串行）；播放开始前确保引擎就绪，未就绪时提前加载，等待期间 F12 可中止执行
- **OCR 等待全程可中断**：引擎未就绪时的等待从"抢初始化锁（不可中断）"改为**轮询就绪标志**——执行前准备、文字守卫、识别文字动作、数字/文字模块、备用文字模块全部接入；首次导入的数十秒内按 F12 随时中止执行，不再出现"F12 后工作线程仍在运行"卡在 `_get_engine`

### 全局检测

- 修复：普通全局行（非引用）配置了跳转目标且「启用触发后跳转」勾选时，命中后**只跳转不点击**——按钮（如结算确定）永远不会被点击，界面不关闭，脚本重复执行时每次重跑都立刻重新触发，形成每轮几秒的触发死循环。现恢复旧引擎语义：**先点击识别位置再跳转**；跳转停用的行保持纯触发（不点击），无跳转的行保持点击识别位置
- 修复：引用模块（脚本内嵌全局模块行 / 工作流步骤）命中后**不再点击**——守卫引擎重构时 `_build_guard_hit` 把 `module_ref` 排除在“点击识别处”兜底之外，而注册阶段又只在“点击自定义位置”时才写入点击坐标，导致模块默认动作“点击识别区域”失效：识别到、日志打出“开始执行处理段”，但没有任何点击。现按模块对象的动作配置恢复点击（`click_match` 点击识别位置并应用 OCR 偏移，`click_custom` 用模块保存的自定义坐标，`continue`/`run_actions` 仍不点击），点击次数/按键与模块配置一致
- 修复：引用模块行配置的跳转目标（`jump_row`/`jump_action_id`）静默失效——「启用触发后跳转」复选框只存在于非引用行对话框，引用行没有开关却被按“默认不勾选”处理，旧脚本里已有的跳转全部失效。引用模块行恢复旧引擎语义：配置了跳转目标即生效（显式写入 `jump_enabled=False` 仍可停用），行摘要同步显示跳转状态
- 修复：引用模块勾选“再执行代码段”后，命中时成功代码段永不执行（`segment_ready` 死分支）——`_build_guard_hit` 直接读取实时刷新的 `success_segment`

### 编辑与保存

- 新增：启动时自动恢复上次关闭时脚本编辑页正在编辑的脚本——应用每次持久化设置（含关闭）都会记录编辑器当前打开的脚本路径（`app_settings.json` 的 `last_script_path`），下次启动自动打开；脚本已被删除/移动时静默跳过，新建/关闭/录制分离后编辑器无脚本则不再恢复
- 修复：脚本/工作流改名保存后遗留旧文件（孤儿文件 + 工作流/引用仍指向陈旧内容）；工作流改名还会静默覆盖同名文件造成数据丢失——改名/新建统一走“绝不覆盖”的去重路径，保存成功后删除旧文件
- 修复：打开脚本不检查未保存修改，直接丢弃编辑且“撤销打开”也无法恢复——与新建/工作流内打开一致地拦截
- 修复：开始录制不检查未保存修改（编辑被静默清空）；录制中保存会写出永远为空内容的“已保存”文件——两个入口均拦截并提示
- 修复：解除窗口绑定不持久化，重启后旧绑定被恢复
- 修复：逐行插入脚本对旧版 `jump_row` 跳转不迁移，插入后跳转目标错位——插入前先 `ensure_action_ids`（与向下复制路径一致）

### 对话框

- 修复：工作流批量设置勾选“不计次数”时 `KeyError('unlimited')`，“应用到全部任务”静默失效
- 修复：添加全局检测对话框的“点击位置”输入行与确定/取消按钮行重叠，控件被遮住无法使用
- 调整：编辑模块对象对话框「持续超过」的“启用持续延时”默认不勾选（时长默认仍为 1000 ms）；模块对象与触发配置中缺失 `hold_enabled` 字段时同样按“不启用”处理
- 修复：把全局模块复制成另一个类别后模块名称显示成 `module:<uuid>`——旧对象没有 `name` 字段（过去以图片路径为键、靠文件名兜底显示），复制后换成 uuid 键导致兜底退化为 uuid；现在加载时按模板文件名补名，复制时对无名字的源对象同样补名
- 新增：脚本内嵌全局模块行（添加全局模块）的「触发后跳转到」可勾选启用/停用，**默认不勾选**——未勾选时模块命中只触发、不跳转（也不点击），脚本继续执行；目标行配置保留，勾选后恢复跳转
- 调整：跳转动作的生效条件选项扩展为“工作流第 2 次或脚本多次执行的第 2 次及以后时跳转”（单独运行脚本重复执行同样适用），**默认勾选该选项**；缺失该字段的动作按“第 2 次及以后生效”处理

### 测试

- 修复三个过时/缺夹具的测试：播放启动的目标激活断言适配每个输入动作前的前台守卫；禁用模块引用守卫补 `_ui` 夹具；`SetFocus` 断言按句柄值比较

## v1.0.0（首个公开发布版本）

按功能模块梳理，不按版本顺序。

### 录制与回放

- 智能轨迹识别：鼠标锁定在窗口中央时记录原始相对轨迹（游戏视角转向，高频采样 ≤16 ms）；离开目标窗口后恢复普通桌面坐标（默认间隔 20 ms，可调 10–500 ms）
- 相对转向回放零额外开销：仅目标窗口变化时才执行窗口激活，激活失败不中断执行
- 录制前自动把目标窗口切换为英语（美国）输入法；F8 新录制自动与已打开脚本分离，保存时自动取不重名
- 回放支持：从指定行开始、按次数重复、断点恢复、连续点击、点击鼠标当前位置（不移动光标）
- 分辨率适配：脚本记录录制电脑的虚拟屏幕尺寸，换机执行时自动缩放坐标

### 脚本编辑

- 动作类型：延时 / 键盘 / 文本 / 鼠标移动 / 点击 / 连续点击 / 滚轮 / 识图 / 识别文字 / 浮动提醒 / 注释 / 引用脚本 / 打开、关闭软件（优雅关闭、管理员权限重试、进程树）/ 跳转 / 前置窗口 / 特殊模块 / 模块引用
- 编辑操作：多选（Shift/Ctrl）、撤销 / 重做、向下复制、从此运行、保存后清除撤销历史
- 插入脚本两种方式：一行引用（执行时实时读取原脚本最新内容）或逐行复制（行 ID 全部重建、跳转引用同步映射，插入后可单独修改）
- 跳转目标按稳定行对象 ID 引用，脚本编辑后仍准确定位；支持“仅在工作流第 2 次及以后生效”的跳转条件
- 工作流步骤可配置“第 2 次及以后从指定行开始”：第 1 次从脚本第 1 行执行准备动作，后续次数直接从核心流程开始

### 识别与模块对象

- 模板匹配识图：全屏 / 绑定窗口 / 自定义区域，阈值与忽略背景，超时后继续 / 跳转 / 停止，命中后点击或跳转，二次识别确认
- PaddleOCR 离线文字识别：按文字内容判断（包含 / 等于 / 任意文字），保留真实文字框坐标，支持向上 / 下 / 左 / 右像素偏移点击、等待文字消失、读取数字（按屏幕坐标拼接为整数）
- 模块对象仓库：切换 / 工作流全局 / 脚本全局 / 特殊四类；按稳定 ID 引用而非图片路径；启用 / 禁用、批量加入 / 批量删除脚本、拼音排序、双击编辑、撤销移除
- 识别设置：区域、阈值、持续时长（命中状态保持设定毫秒后触发）、开始识别前延时（ms / s / min）、备用识别模块（命中后点击或不点击）、连续点击次数与间隔
- 代码段：命中附加代码段、未识别超时代码段，支持跳转、连续点击、结束当前最里层脚本、重新执行工作流等特殊动作
- 点击屏幕选取坐标时自动隐藏点击窗口及其全部上级窗口，选取结束后恢复，避免遮挡目标页面

### 工作流

- 脚本步骤：执行次数（剩余次数制）、不计次数、开始前等待、重复间隔、启用 / 禁用、启动延时（0–86400 秒）
- 工作流全局模块：按 step_id 独立管理，每轮共享同一张截图按序检测；持续命中触发、超时代码段；触发后可结束当前脚本 / 重新执行工作流，并从断点恢复
- 重新执行工作流：支持配置跳转行对象（模块配置表单 / 代码段动作编辑两处可设），未指定时用全局默认跳转行（“设为默认”保存）；已移除工作流级“统一重启目标”
- 脚本全局模块：严格跟随脚本作用域进入 / 离开，每次重复重新注册，超时计时从本次重复开始计算
- 测试模式（不扣减次数）、从选中行运行、单独执行一次测试；剩余次数与跳转行严格一致，断点恢复沿用原开始时间

### 系统与稳定性

- 强制专注模式：切换英语输入法 + 系统级键鼠锁定（BlockInput），阻止误操作；F12 保留紧急停止
- 异常收尾：停止或异常退出时释放仍按住的按键与鼠标键，同时停止并清除全局检测
- 执行小窗：录制 / 执行双模式置顶小窗，不抢占目标窗口焦点；显示进度、动作与 OCR 识别日志
- 运行日志按日期与启动会话拆分落盘，界面繁忙不丢日志；日志页可直接打开目录
- 定时备份（1h / 1天 / 1周）、开机自启动、启动到托盘、启动自动执行指定工作流
- 打包版 OCR 引擎外置：主程序约 70 MB，`paddle_ocr` 目录（引擎、模型与全部运行依赖）与 exe 同目录按需加载；构建自动补全依赖闭包，并校验版本、符号与输入锁

# MacroFlow Studio 更新历史

- 1.0.0：首个公开发布版本（合并 1.87.1 之后的全部修复，按语义化版本重新编号）。修复打包版 OCR 运行时缺失 `http.cookies`、`unittest.mock` 等标准库导致“识别文字”报错的问题，构建补全依赖闭包并保持稳定；删除大漠（dm）后台键鼠与后台点击（PostMessage）功能及对应界面与设置项；修复执行小窗滚动时目标游戏窗口失焦的问题；修复相对转向动作变慢的回归（仅目标窗口变化时才执行窗口激活，恢复旧版转向速度）；全局检测“持续时长”上限由 60 秒放宽到 10 分钟；修复脚本按次数重复时“剩余 N 次”日志与跳转行不一致的缺陷。
- 1.87.1：修复打包版 OCR 引擎加载失败（报 “缺少 PaddleOCR 依赖 (No module named 'colorlog')”）。外置 `paddle_ocr/` 目录补齐 paddleocr 的全部运行时第三方依赖（colorlog、setuptools、google/protobuf 等约 57 个包及其 .libs / dist-info 元数据）；保留 paddle/_typing（paddle 运行时真实依赖，此前被体积清理误删）；paddlex 的 ocr-core 附加依赖元数据检查（pyclipper / pypdfium2 / python-bidi / shapely）一并就位。构建脚本与构建校验同步更新，后续构建自动复制全部依赖。
- 1.87.0：工作流恢复模板改为普通工作流文件。删除“恢复模板 / 保存模板 / 新建工作流模板 / 复制当前工作流为新模板 / 一键恢复 / 删除模板”整套功能；工作流页显示的就是当前文件内容，打开修改后保存即覆盖该文件。旧模板首次启动自动迁移为 `workflows` 下的工作流文件（不覆盖已有同名文件，旧文件改名 `workflow_templates.migrated.json` 保留备份）。OCR 引擎外置：paddle 全家不再打进 EXE，改为 exe 同目录的 `paddle_ocr/` 目录按需加载（首次使用“识别文字”时才加载），主程序体积约 253 MB → 约 70 MB、启动显著加快；构建同时移除仅 paddle 使用的 shapely / pyclipper / imagesize / pypdfium2 / python-bidi 依赖。
- 1.86.14：恢复进入关卡后再绑定战斗窗口的执行方式；工作流启动时战斗窗口尚未创建也不影响后续动作，第一条相对转向执行时会按保存的目标身份重新查找并缓存新窗口。执行异常收尾现在同时停止并清除全局检测，避免主界面恢复后 OCR 仍继续运行。
- 1.86.13：修复脚本全局模块代码段执行“结束当前最里层脚本”时错误跳到工作流下一行；现在只结束当前重复轮次、扣减本次并从同一工作流行开始下一次，剩余次数用完后才进入下一行。全局模块显式选择“直接结束当前脚本，执行工作流下一项”的行为保持不变。
- 1.86.12：解除侧栏目标窗口与普通工作流动作的错误耦合；旧窗口句柄失效时普通点击、键盘、识图和 OCR 继续执行，仅相对转向和明确选择“窗口区域”的识别动作要求重新绑定。
- 1.86.11：执行异常时先记录“即将关闭执行小窗并恢复主界面”再统一收尾；Windows 打包改为不依赖 COMCTL32 序数 380 的 bootloader，并在构建校验中阻止该导入回归。
- 1.86.10：修复双击模块后编辑窗口初始化异常、界面表现为卡死的问题；备用点击次数与间隔的双输入控件现在正确创建并显示。
- 1.86.9：备用识别模块命中后新增独立的连续点击次数和点击间隔（毫秒）设置，默认 1 次、100 ms；运行时实时读取模块配置。
- 1.86.8：图片和文字识别模块新增可选“备用识别模块”。主模块等待未超时时，每轮优先识别主模块并同时检查备用模块；主模块命中立即结束当前模块，备用模块命中可选择点击其命中位置或不点击，随后继续识别主模块。备用目标持续存在时只处理一次，消失后再次出现才重新处理；备用配置保存稳定模块 ID，并使用备用模块自己的识别方式、区域、阈值和文字条件。
- 1.86.7：脚本全局模块新增“开始识别前延时”，支持 ms / s / min。脚本进入作用域后先等待指定时间，再启动图片或文字识别；延时可被停止操作中断，且不计入未识别超时。
- 1.86.6：提高工作流全局模块区的默认高度和分隔权重，启动时可显示约 5–6 行全局模块；仍可拖动中间分隔线自由调整上下区域。
- 1.86.5：重新排版工作流页面。文件操作与重启跳转合并，恢复模板保持独立一行，步骤按钮按“添加 / 插入”和“编辑 / 排序”分组；全局模块与工作流步骤改为可拖动的上下分隔区，默认压缩全局模块高度并扩大工作流步骤列表。
- 1.86.4：工作流恢复模板新增“保存模板”“新建工作流模板”“复制当前工作流为新模板”。已打开模板可直接按原名称覆盖保存，不再重复输入名称；新建和复制时提供名称输入框，新建得到空白模板，复制保留当前工作流的全部配置。
- 1.86.3：修复模块代码段的点击 / 连续点击选取坐标后界面可能全白；上层窗口改为临时透明，避免嵌套 Tk 窗口反复隐藏和恢复导致 Windows 客户区不重绘。
- 1.86.2：模块对象的“附加代码段”和“未识别超时代码段”新增“连续点击”；代码段中的点击和连续点击使用“点击屏幕选取”时会隐藏点击窗口、模块编辑窗口及其全部上级窗口，选取结束后按原状态恢复，避免 MacroFlow 窗口遮挡其他页面坐标。

- 1.86.1：修正读取数字模块的结果措辞。成功读取但数字不相等时记录“比较结果：不相等”，超时未读取到数字时记录“读取结果：未读取到数字”，不再把两种正常分支结果统一称为“失败”；普通图片和文字模块原有的成功/失败结果措辞保持不变。

- 1.86.0：工作流新增恢复模板。当前工作流的工作流全局模块、脚本与模块步骤、排列顺序、执行次数、不计次数、开始前等待、重复间隔、启用状态、启动延时和统一重启目标可保存成多个命名模板；选择模板后可一键恢复，恢复内容先作为未保存工作流载入，避免直接覆盖原工作流文件。模块仍保存实时引用，具体识别和动作配置继续由模块仓库统一管理。

- 1.85.1：新插入的“读取数字”模块将“不等于或未读取到时”默认设为“继续下一行”；“等于时”仍默认跳转，已有脚本中已保存的两路选择不会被改写。

- 1.85.0：切换模块新增“读取数字”识别方式。模块在指定区域运行离线 OCR，把各数字框按屏幕 x 坐标从左到右拼接为整数（支持一个框多位、全角数字和前导零）；插入脚本时为当前引用行设置比较数字，等于复用成功分支，不等于复用失败分支，两路继续使用稳定行对象跳转。未读取到数字会按模块对象的阻塞设置与未识别时限重试，超时后走失败分支；数字读取不执行图片匹配、点击或模块代码段，也不会作为工作流模块行或批量加入脚本。

- 1.84.99：脚本中的每个切换模块引用行新增独立结果分支。模块识别及模块对象动作完成后明确产生成功信号，非阻塞模块连续未识别达到模块对象的“未识别时限”后产生失败信号；当前行可分别设置“继续下一行 / 跳转到行对象 / 结束当前最里层脚本”，默认成功和失败均继续。跳转仍保存稳定 `action_id`，目标行移动后不会失效；模块对象继续统一管理识别方式、区域、相似度、阻塞、点击与代码段，行级分支不会影响其他引用。

- 1.84.98：修复关卡封装“执行 x 次”时脚本全局模块只在首次重复注册的问题。之前整个 x 次只注册一次全局检测监控，超时（如“超时10min返回大厅”）从整个运行起点连续计时；现在每次重复都会重新进入脚本全局作用域——先退出上一次注册的全局模块，再注册本次的，超时等计时从本次重复开始重新计算。

- 1.84.97：鼠标点击、鼠标移动、连续点击动作的“屏幕 X / 屏幕 Y”改为可直接输入（“点击屏幕选取…”按钮保留），并修复“执行前延时”输入框被误设为只读、点击无法输入的问题。坐标输入框只读是旧设计，只允许幕布选取；本次同时放开手动输入。

- 1.84.96：侧栏“桌面轨迹间隔”和“脚本重复次数”改为标题、控件两行布局，并加宽“修改/保存”按钮。原单行把标题、帮助徽章、数字框、单位框和按钮挤在 318px 内，高 DPI（打包版按真实 DPI 渲染）下整行溢出侧栏，最左侧的“修改/保存”按钮文字被裁切、显示不清楚。

- 1.84.95：工作流页面右键单个脚本新增“单独执行一次测试”。直接加载该行脚本执行 1 次，不影响工作流的剩余次数和进度，方便单独验证某个脚本；使用该脚本自己的前置窗口与录屏设置，目标窗口沿用当前绑定。

- 1.84.94：所有延时相关的时间字段单位新增“分钟（min）”。与已有的 `ms / s` 一样可在下拉框切换，切换时自动换算显示值，最长 1440 分钟；脚本和工作流仍统一保存为毫秒，旧数据无需迁移。

- 1.84.93：所有可编辑时间字段统一支持 `ms / s` 单位选择，切换单位时自动换算显示值，脚本和工作流仍统一保存为毫秒，旧数据无需迁移。模块的“无需识图”扩展到脚本全局与工作流全局模块：不截图、不匹配模板、不运行 OCR，只按“未识别时限”计时，到时执行超时代码段；切换模块仍是执行到即运行。

- 1.84.92：模块对象的“识别方式”新增“无需识图”。切换模块执行到该引用时不截图、不做模板匹配或 OCR，直接按延时执行“成功后继续 / 点击自定义位置”和可选附加代码段；无需模板图片与框选区域。工作流全局、脚本全局仍属于常驻检测模块，不接受无检测条件。

- 1.84.91：模块引用位置窗口支持多选，并新增“删除选中引用”；可只删除指定的几个引用，未选中的引用保持不变。

- 1.84.90：模块管理新增“查看引用位置”和“删除全部引用”。可按顺序跳转到每个脚本中的模块引用行，也可一次删除当前配置脚本中的全部引用。

- 1.84.89：录制小窗与执行小窗共用同一个可调节位置；调节一次后，录制和执行都会使用该位置。

- 1.84.88：执行小窗新增“调节执行小窗位置（显示边界）”功能。可拖动带红色边界的预览窗口，确认后保存位置，后续执行小窗会使用该位置并自动限制在屏幕范围内。

- 1.84.87：修复右键打开引用脚本时第二个 EXE 因复用 PyInstaller 临时目录而报 Tcl/Tk `tcl_data` 不存在的问题。启动新窗口时会清理 `_MEIPASS`/`_MEIPASS2` 并强制重新解压运行目录。

- 1.84.86：修复脚本文件在目录中改名后，通过工作流引用右键打开时仍显示 JSON 内旧脚本名称的问题。打开脚本现在以实际文件名同步编辑器名称，保存不会再写回旧名称。

最新版本说明放在最前。功能使用说明见 README.md。

- 1.84.85：修复模块管理中禁用工作流全局模块后，工作流页面仍显示“✓ 全局”且运行时仍可能启动检测的问题。全局模块行现在同时检查行自身状态和模块对象仓库的实时状态：对象禁用时显示“模块已禁用”，对象缺失时显示“模块不存在”，两种情况都使用禁用样式并在执行日志中说明跳过原因；禁用行不会等待开始前延时。监控注册入口再次校验对象状态，避免排队期间状态变化后仍启动检测。
- 1.84.84：界面日志与文件日志统一为同一条记录。后台线程产生日志时先同步追加并刷新到 `logs/日期/MacroFlow_时间_进程.log`，随后再排队更新运行日志页，避免 UI 阻塞或异常退出导致已产生的记录未落盘。工作流页面新增默认关闭的“启动延时”开关和秒数输入框，支持 0–86400 整数秒；从头、从选中行以及启动时自动运行都会在排期时间到达后等待该延时，F12 可中止等待；全局模块断点恢复和特殊模块内部重启不重复延时。
- 1.84.83：模块对象的点击类成功动作新增“点击次数”，默认 1，范围 1–9999。点击识别区域、自定义位置及二次识别点击全部支持；普通模块、脚本全局与工作流全局模块实时读取同一配置，并在日志中显示多击次数。
- 1.84.82：空白脚本的鼠标轨迹间隔固定恢复为 20 ms，不再受旧版全局应用设置中的 100 ms 影响；已有脚本仍读取自身保存值。模块对象的模板图片识别新增“直到区域内检测不到期望文字才完成”，模板存在时循环执行成功动作，消失后才完成；全局模板模块则按“先出现、后消失”边沿触发，并关闭未识别超时计时。
- 1.84.81：“重新执行工作流”的目标改为当前工作流统一设置。工作流页面直接提供“所有重新执行工作流统一跳转到”行对象下拉框，脚本、普通模块和全局模块中的所有触发均读取同一个稳定行对象 ID；删除动作级目标弹窗和动作级目标配置。
- 1.84.80：工作流中的“重新执行工作流”特殊模块可在工作流界面设置目标行对象，按稳定的行对象 ID 跳转；模块对象管理中的启用/禁用状态会同步显示在工作流行状态，并跳过已禁用模块。

- 1.84.79：模块对象管理的“特殊代码段”标记改为只检查已启用的代码段。编辑模块对象时必须勾选“附加代码段”或“未识别超时代码段”，对应代码段中的特殊模块才会触发紫色名称标记、紫色选中高亮和具体名称说明；取消勾选后立即恢复普通显示，即使代码段内容仍被保留也不再误报。
- 1.84.78：特殊模块“结束当前脚本，执行工作流下一项”更名为“结束当前最里层脚本，继续执行”，避免把调用方的后续行为误写成该动作固定跳到工作流下一项。模块对象管理为全部、切换、工作流全局和脚本全局列表新增“代码段特殊模块”列；成功附加代码段或未识别超时代码段保存了固定特殊动作时，名称增加紫色“特殊代码段”标记，列内按“附加/超时”注明每个特殊模块的具体名称，选中时使用独立紫色高亮。
- 1.84.77：恢复并强化跳转动作的工作流重复条件配置。添加/编辑跳转动作现在用“跳转生效条件”明确二选一：每次执行均跳转，或仅在该脚本于工作流中执行到第 2 次及以后时跳转；第 1 次与单独运行时继续下一行，跳转目标继续使用稳定的动作 ID 引用指定行对象。
- 1.84.76：识别配置收敛到模块对象这一套模型。脚本新增动作区移除独立“识图”“识别文字”，统一使用“识别模块”创建或选择模板识别/OCR 模块；模块代码段菜单同步删除两套原始识别入口。脚本中的模块引用只保留进入前和完成后的引用级延时，其余识别与动作配置统一在模块管理中维护并实时生效。
- 1.84.75：修复工作流直接模块行偶尔显示“模块 未设置”。模块引用动作会持久化所选模块名称，列表刷新不再依赖选择器的临时状态；已有行仍可按模块引用键或模板文件名显示名称。
- 1.84.74：普通工作流支持直接添加或按选中行上下位置插入启用的切换模块，不再要求先把模块包装成脚本。模块作为独立工作流行保存和执行，并支持拖动排序、执行次数、开始前等待、重复间隔、启用/禁用、从选中行运行及测试模式。
- 1.84.73：工作流不再因已配置的前置窗口未打开而整体失败。入口默认前置窗口或步骤脚本自己的前置窗口无法解析时，记录警告并跳过该前置条件，当前脚本和后续工作流继续执行；单独运行脚本的严格校验保持不变。
- 1.84.72：将工作流“测试运行”改为独立的“测试模式”勾选项。勾选状态同时作用于“运行工作流”和“从选中行运行”，可以先选起始行再用测试规则运行；取消勾选恢复正常次数执行。
- 1.84.71：新增工作流“测试运行”按钮。测试模式下，普通计次行按 `min(剩余次数, 1)` 执行且不扣减原次数，次数为 0 的行继续跳过；不计次数行不受计次行耗尽判断影响并保持执行。修复执行小窗计时在每次全局模块断点恢复时被重置、刷新循环随旧 worker 结束而停在 `00:00` 的问题：现在只有用户或启动项真正开始工作流时归零，内部恢复沿用原始起点并重启刷新。修复目标窗口明明位于前台但窗口区域识别仍使用旧 HWND 的问题：当前前台窗口只要类名/进程身份匹配就会被优先重新绑定，标题动态变化不再判错，并在日志记录 HWND 变化。
- 1.84.70：脚本全局模块现在拥有严格的当前脚本生命周期。播放器进入脚本（含被引用脚本）时会先按各自 `action_id` 启用全部脚本全局模块，离开时只注销该脚本的监控，不影响工作流全局模块；因此从指定行开始、工作流第 2 次起跳行和全局触发后的断点恢复都不会漏掉起始行之前的脚本全局模块。脚本“跳转”动作新增可选条件“仅在工作流中执行到第 2 次及以后时跳转”，第 1 次和独立运行脚本时条件不成立并继续下一行。
- 1.84.69：修复同一脚本中的多个脚本全局动作共用单一监控身份，导致后启用动作替换前一个、重触发锁彼此干扰的问题。现在每个脚本全局动作使用自己的稳定 `action_id` 注册独立监控并维护独立触发状态/重触发锁；工作流全局模块使用 `workflow:<step_id>`，脚本全局动作使用 `script:<action_id>`，两类身份也不会冲突。
- 1.84.68：修复启用“大漠转向兼容”后，系统 `dm.dmsoft` COM 注册缺失/损坏或位数不匹配会导致整个工作流报“Automation 服务器不能创建对象”并失败的问题。现在首次启动或调用大漠失败后会关闭损坏桥接，本次执行自动回退到默认相对鼠标方式，鼠标按键和滚轮调用失败也会降级；日志明确标识 COM 注册问题，下一次执行重新检测插件。
- 1.84.67：动作编辑器的普通“识图”动作在“超时后”下拉框新增“结束当前脚本”。顶层脚本超时后跳过剩余动作并进入工作流下一项，单独运行时直接结束；引用脚本内部超时时只退出当前引用脚本，随后继续外层脚本。下拉框同时改为完整中文选项：“继续执行 / 跳转到目标动作 / 结束当前脚本 / 停止全部执行”。
- 1.84.66：定时备份间隔由可填写分钟数改为只读下拉选择，仅提供 `1h`、`1天`、`1周` 三项；旧的 `backup_interval_minutes` 配置字段已移除，设置统一保存为 `backup_interval`。
- 1.84.65：侧边栏新增“启动与备份”设置：可按 1–1440 分钟定时备份全部脚本，每个源脚本只保留并覆盖一份固定备份；可选择当前用户开机自启动、启动时最小化到托盘，以及选择一个工作流在启动后自动加载执行。启动工作流缺失或损坏只记录错误，不影响程序启动。运行日志现在自动保存到 `logs/日期/`，每次启动独立生成一个日志文件，避免所有记录堆积到单一文件；日志页可直接打开日志目录。
- 1.84.64：所有 OCR 文字检测都会在运行日志和执行小窗显示实际识别内容，即使没有命中期望文字；输出同时包含模块或动作名称、实际文字、期望文字以及“命中 / 未命中”状态。普通文字模块、独立识别文字动作、工作流全局和脚本全局文字模块均生效；相同结果持续出现时自动去重，文字或命中状态变化后才追加记录，避免轮询刷屏。
- 1.84.63：识别文字模块的“文字点击偏移”新增“拖拽选取…”。在幕布上的起点按住鼠标左键不松开，拖到终点后松开，即可用两个点自动计算偏移；拖拽过程中实时显示带正负号的 dx、dy，保存后自动换算为向上、向下、向左、向右像素。四项输入框继续保留，可用于精确微调。
- 1.84.62：识别文字模块现在保留 PaddleOCR 返回的真实文字框坐标，“点击识别区域”会点击命中文字中心，不再错误点击整个框选区域中心；新增向上、向下、向左、向右四项像素偏移。修正“等待文字消失”的循环语义：每次识别到期望文字都会按偏移执行点击及附加动作，然后重新截图识别，直到区域内没有期望文字才退出。
- 1.84.61：模块对象的“识别文字”新增“等待文字消失”。勾选后，普通模块会持续识别框选区域，只要期望文字仍存在就继续循环，直到目标文字消失才完成并执行成功动作；该等待不受普通识别超时限制，F12 仍可停止。工作流全局和脚本全局文字模块会先确认目标文字出现，再在文字消失时触发，避免启动时因文字原本不存在而误触发。
- 1.84.60：修复模块对象管理使用独立选中样式后列表出现白底、文字不可见的问题。三套模块列表样式现在完整复制基础 Treeview 布局，并明确设置深色背景、输入区域背景、正常文字、字体与行高，然后再分别应用启用绿色和禁用红褐色选中高亮。
- 1.84.59：修复模块对象管理中启用与禁用模块的选中高亮没有真正呈现不同颜色。选中行不再动态改写同一 Treeview 样式，而是直接切换启用绿色、禁用红褐色、无选择默认蓝色三套独立样式，确保 ttkbootstrap 主题下也会立即重绘。
- 1.84.58：模块对象管理的选中高亮按启用状态区分颜色：启用模块选中时显示绿色高亮，禁用模块选中时显示红褐色高亮，切换选择后立即刷新，避免仅靠文字判断当前模块状态。
- 1.84.57：模块对象管理新增“启用 / 禁用”仓库开关。禁用模块显示灰色 `【已禁用】` 标识，并从脚本、工作流及模块代码段的插入选择器中隐藏；批量加入脚本也会拒绝禁用模块。已有脚本和工作流中的模块引用继续执行，不会因仓库禁用而失效或被删除。
- 1.84.56：模块管理的“全部模块 / 切换 / 工作流全局 / 脚本全局”列表会特殊标识勾选“阻塞识别”的模块，模块名称前显示黄色 `【阻塞识别】`，无需进入编辑窗口即可识别无限等待模块。
- 1.84.55：模块内部的成功后代码段和未识别超时代码段会特殊标识所有阻塞识别动作。引用到开启“阻塞识别”的模块，或代码段内自身开启阻塞的识图动作，列表行都会显示黄色 `【阻塞等待】` 标识，便于提前发现可能无限等待的位置。
- 1.84.54：工作流全局与脚本全局模块的“持续超过”新增“启用持续延时”复选框：勾选时命中状态持续达到设定毫秒数后触发，不勾选则第一次识别命中立即执行，原毫秒值会保留。工作流全局检测改为统一顺序调度：每轮只截一张共享截图，严格按列表顺序逐个检测；任一模块触发或进入未识别超时代码段后立即冻结后续检测，等待该模块完整执行并恢复工作流后才重新开始调度。图片与文字识别模块都使用本轮共享截图。
- 1.84.53：工作流页的“工作流”和“工作流全局模块”列表均支持 `Ctrl+A` 全选和鼠标多选。多选后可批量删除并逐项撤销，也可统一启用/禁用；编辑、移动和拖拽仍以首个选中项处理。
- 1.84.52：脚本动作列表和模块内部代码段列表均支持 `Ctrl+A` 全选。模块内部代码段改为真正的多选列表，全选或多选后点击“移除”会一次删除全部选中动作；编辑、上移、下移仍以首个选中动作处理。
- 1.84.51：工作流全局与脚本全局模块允许保留同名、同配置的独立副本；在模块对象管理的“工作流全局”“脚本全局”或“全部模块”列表右键任一全局模块，可直接改成或复制成另一种全局类型。复制会生成独立模块 ID，两份之后可分别编辑，只按所属类别决定触发时机。
- 1.84.50：模块对象管理新增「批量从脚本删除」。选中模块后点击该按钮，勾选脚本即可把脚本中所有引用该模块的动作行一次性删除；对话框按脚本列出该模块的引用行数（含该模块的脚本默认勾选、未使用的标"未使用"），脚本其余内容保持不变。与「批量加入脚本」互为反向操作，批量加入误加时可一键批量撤回。
- 1.84.49：修复关闭软件对话框底部按钮看不见的问题。对话框原为固定高度，打包版按显示器真实 DPI 渲染（如 125% / 150% 缩放）时表单内容实际高度超过窗口，底部的确定 / 取消按钮被挤出窗口外完全看不到；现在按内容实际需求自动调整窗口高度并居中，任何 DPI 下按钮都可见。
- 1.84.48：插入脚本做成两个独立按钮。脚本编辑工具栏「插入脚本」拆为「⇥ 引用脚本」（插入一行引用动作，执行时实时读取原脚本最新内容）与「⇥ 逐行插入」（把脚本每一行都复制到当前位置，行 ID 全部重建、跳转引用同步映射到新 ID，插入后可单独修改、与原脚本不再关联），不再弹方式选择对话框，少一步操作。
- 1.84.47：桌面轨迹间隔默认值从 100 ms 调整为 20 ms（可调范围不变，仍为 10–500 ms）。新录制桌面移动的轨迹点更密、回放更精细；已保存的设置与脚本里显式记录的间隔不受影响。
- 1.84.46：插入脚本支持两种方式：一行引用（执行时实时读取原脚本最新内容）或逐行复制（行 ID 全部重建、跳转引用同步映射，插入后可单独修改、与原脚本不再关联）。
- 1.84.45：模块触发/超时信息写入运行日志。脚本内识图引用模块时，模块超时（如"专注"20 秒未识别到）、模块点击、附加代码段执行等关键操作，现在会同时写入运行日志并标明模块名与触发原因，之前这些信息只显示在状态栏小窗、运行日志里看不到，排查"为什么重启/为什么点击"时无从下手。
- 1.84.44：关闭软件动作大幅增强，之前"结束不掉"的进程现在能结束了：1）优雅关闭请求被拒绝（如目标以管理员身份运行）时不再干等，直接进入强制结束；2）强制结束自动重试 3 次（此前只试 1 次）；3）结束后轮询确认改为每次 1 秒短轮询，不再一次性空等 3 秒；4）新增"以管理员权限重试"选项（默认勾选）：普通权限反复失败后通过 UAC 提权结束进程（会弹出授权窗口，取消则如实报失败）；5）新增"连同子进程一起结束（进程树）"选项（默认不勾选）。关闭软件对话框同步增加这两个选项，脚本摘要显示"进程树 / 管理员重试"标记；另修复代码段列表中关闭/打开软件行显示"?"的问题。预置"断线重连"全局模块的关闭软件动作已启用管理员重试。
- 1.84.43：点击动作新增“点击鼠标当前位置”模式。添加/编辑点击动作时勾选“点击鼠标当前位置（执行时不移动鼠标）”，执行时鼠标在哪就在哪点击，不移动光标、不做分辨率缩放，适合接在前一个动作（如识图命中）之后点击其当前位置的场景。脚本摘要与跳转下拉中显示“鼠标当前位置”。
- 1.84.42：跳转目标下拉（识图跳转、跳转动作、"第 2 次起从指定行"的起始行等）显示更详细的行信息：点击/连续点击显示按键和坐标、键盘显示键名、鼠标移动显示相对偏移、滚轮显示方向与位置、引用脚本显示脚本文件名、打开/关闭软件显示软件名、跳转显示目标行、延时显示等待时长，并补全了"结束脚本/重启工作流"等缺失的类型标签；"设置执行次数"对话框标题显示当前脚本文件名。下拉选项一眼可辨，不用再猜点的是哪一行。
- 1.84.41：安装包瘦身与启动提速（功能不变）。移除打包内容里冗余的 DLL 副本（paddle 的 phi/mkldnn 等与包内完全相同的根目录副本约 31MB，运行时由 paddle/numpy/shapely 各自的 DLL 目录加载）、OpenCV 视频解码与级联数据（软件只用图像匹配）、paddle 的 C++ 头文件与类型标注、以及运行时不可达的 pip/networkx/hf_xet 依赖，安装包从约 293MB 减至约 **250MB**，启动实测约快 30-40%（19 秒 → 11 秒量级）。发布门禁新增 `unpack_packaged.py` + `test_packaged_ocr.py`：解出 EXE 全部内容后在去重后的文件布局下用真实模型跑一次 OCR 识别，通过才发布。
- 1.84.40：工作流步骤（封装关卡等）配置执行多次时，**第 2 次及以后可从指定行开始**。双击工作流"次数"列打开设置，勾选"第 2 次及以后从指定行开始"并选择起始行：第 1 次始终从脚本第 1 行执行（准备动作只做一次），第 2-N 次直接从指定行进入核心流程。按行对象（动作 ID）引用，脚本编辑后仍能准确定位；断点恢复优先于该设置；不计次数（每轮只执行 1 次）时此项不生效。
- 1.84.39：执行/录制小窗重新显示时不再抢激活窗口。此前窗口已存在时（如执行中再次弹出小窗）走 Tk 的 `deiconify` 显示路径，该路径会把窗口 Restack 到最前并激活它，导致游戏窗口失去激活、独占全屏游戏退出全屏；现在窗口保持可见置顶，无需重新显示时不做任何操作，万一被隐藏时用 `SW_SHOWNOACTIVATE` 不激活地显示，游戏前台全程不受影响。
- 1.84.38：修复全局模块触发时“跳转到当前脚本最后一行”跳错脚本的问题。触发时正处于被引用关卡脚本内部（封装关卡脚本引用了关卡脚本）时，模块代码段的“结束当前脚本”现在跳到**最内层关卡脚本**的最后一行执行，外层封装脚本从引用动作之后继续，而不是跳到封装脚本最后一行、跳过后续动作。
- 1.84.37：源码运行时识别文字报“缺少 PaddleOCR 依赖”的问题：默认 Python 只装了界面/输入类依赖、没有 paddle 全家，OCR 引擎无法导入。新增 **run.bat** 启动器（使用构建环境 C:\Python313 + .deps 启动），报错信息同步带上真实原因，不再误导性提示“请重新安装软件”。
- 1.84.36：模块对象（切换 / 工作流全局 / 脚本全局）新增**识别文字方式**：识别方式可选“模板图片 / 识别文字”，识别文字按内容匹配（期望文字留空=识别到任意文字即命中，支持包含 / 等于），无需模板图片、区域可留空（全屏）。文字命中没有精确位置，“点击识别区域”等点击动作落在识别区域中心（全屏为主屏中心）；全局检测同样支持文字模块，命中触发、消失确认、超时逻辑与图片模块一致。“识别文字”动作的“显示识别结果浮动提醒”默认勾选，每次识别结果直接可见。
- 1.84.35：识别文字引擎更换为 **PaddleOCR**（paddlepaddle 3.3 + PP-OCRv5 模型，纯离线），解决此前内置 OCR 引擎在打包版中无法使用的问题。中文与数字识别更准（1080p 全屏约 2.5 秒，普通小区域约 0.3 秒）；“识别文字”动作的用法与设置不变。
- 1.84.34：新增“识别文字”动作（脚本工具栏“◪ 识别文字”），按**文字内容**做判断，与模板识图互补（模板找固定图案，OCR 读会变化的文字，如提示语、弹窗文案）。可框选识别区域（留空=全屏）或绑定目标窗口，填写期望文字后按“包含 / 等于”匹配，留空则识别到任意文字即命中；支持轮询等待与超时后的继续/跳转/停止。识别结果会显示在状态栏和执行小窗。
- 1.84.33：识图新增“忽略背景”开关（识图动作与模块对象表单均可用）。开启后只按模板上的文字/图形笔画匹配，背景颜色、纹理、高亮变化都不影响识别结果，适合按钮背景会变色、高亮或置灰、导致普通识图相似度骤降的场景；模板背景无法自动识别（如渐变背景）时自动回退普通匹配。
- 1.84.32：执行小窗新增独立开关。左侧设置里“执行时显示悬浮小窗”与“录制时显示悬浮小窗”分开勾选，执行（普通脚本或工作流）时是否弹出小窗可单独控制；取消勾选后执行照常进行，只是不显示小窗。
- 1.84.31：执行/录制小窗在**首次弹出**时不再抢激活窗口。此前 Tk 映射新顶层窗口时会先激活它，`WS_EX_NOACTIVATE` 样式在窗口显示后才应用，小窗弹出的瞬间会抢走一次激活；现在样式在窗口首次显示之前就设置好，滚动日志、每 250 ms 的状态刷新本来就只更新文本、不会触发激活。
- 1.84.30：强制专注模式（切换英语输入法 + 系统输入锁）只在工作流**首次开始**时设置一次。全局模块中断后的断点恢复、特殊模块“重新执行工作流”都沿用第一次建立的输入锁，不再重复切换输入法或重新锁定，避免中途重置输入状态。
- 1.84.30：修复全局模块触发点击后“又触发一次”的问题。当焦点不在目标（游戏）窗口上时，模块的桌面级点击会落在该坐标最上层的其他窗口上，激活的窗口随之改变——游戏画面可能被遮挡或短暂消失，触发锁被解除后同一模块再次触发。现在点击点落在绑定窗口范围内且窗口不在前台时，点击前先把目标窗口置于前台，保证点击到达游戏、画面按预期推进。
- 1.84.29：脚本类别恢复为三个且只保留“关卡 / 关卡封装 / 切换”，目录设置中同步移除“工作流全局 / 脚本全局”。这两个名称现在只属于模块类别：模块对象管理包含“切换 / 工作流全局 / 脚本全局 / 特殊”；工作流只选择工作流全局模块，脚本编辑器只插入脚本全局模块。原 `global` 模块对象迁移为 `workflow_global`，已有 7 个工作流检测模块数据完整保留。
- 1.84.28：脚本分类中的原“全局”更名为“工作流全局”，并新增独立“脚本全局”分类及保存目录。模块对象管理同步使用“工作流全局模块”名称；工作流选择已有全局脚本时只读取“工作流全局”目录。旧 `scripts/全局` 路径会自动兼容到 `scripts/工作流全局`，已有工作流引用不失效。

---


- 模块对象的“主动作完成后执行的代码段”和独立未识别超时代码段新增“跳转到当前脚本最后一行”。执行到该动作会立即停止代码段剩余动作，并从调用该模块的当前脚本实际最后一行动作继续；工作流全局模块会把保存的恢复断点改到当前脚本末行。该功能不同于“结束当前脚本，执行工作流下一项”，最后一行动作会真实执行；如果模块本来就在末行，也不会再次执行自身形成循环。

- 修复强制专注工作流被全局模块打断时点击失败：当前脚本停止保存断点后，不再提前关闭专注模式的 `SendInput` 转发线程；输入锁会跨越“中断、执行全局模块、恢复原脚本”全过程持续生效。只有工作流真正结束、无法继续或用户按 F12 时才释放，避免出现“强制专注模式已停止，回放动作未发送”。

- 模块对象现在使用独立身份，不再把模板图片路径当作模块本身。同一张图片可以同时建立多个不同名称的切换模块或全局模块；每个模块分别保存类别、识别区域、阈值、持续时间、成功动作、成功后代码段和未识别超时代码段，编辑或执行其中一个不会覆盖其他模块。脚本及工作流引用保存模块 ID，同时保留实际图片路径用于显示和旧数据兼容。

- 模块对象新增独立的“超过时限未识别执行代码段”：可设置连续未识别毫秒数和专属代码段，不会与原有成功后附加代码段混用。切换模块达到时限后执行超时段再继续原脚本；全局模块达到时限时暂停当前工作流、完整执行超时段后恢复断点。成功后代码段与超时代码段的“添加…”菜单都新增“前置指定窗口…”，保存稳定窗口特征，执行时重新查找并前置当前窗口。
- 全局检测在工作流整个执行期间持续运行，不是只在开头检测一次。所有已启用的全局模块改为共享同一轮全屏截图，再各自在内存中按自己的模板、区域、阈值和检测间隔匹配，避免每个模块重复截屏造成卡顿。

- 修复模块引用明明存在却显示“对象不存在，按内嵌参数执行”的路径错误：配置中的相对图片/脚本路径现在始终以程序或 EXE 所在目录为基准，不再受快捷方式“起始位置”、终端当前目录或启动方式影响。模块仓库查询、编辑器摘要和运行时会使用同一个规范化路径。

- 脚本编辑器中双击识图模块或全局模块引用时，“编辑模块引用”窗口新增“替换模块…”，可从相同分类重新选择其他模块；双击引用脚本时按钮明确改为“替换脚本…”。替换会保留当前行的稳定动作 ID、执行前/后延时以及全局模块跳转目标，不会破坏已有跳转引用或行级设置。

- 主窗口“运行日志”和执行/录制悬浮窗的滚动日志现在会在**每一行**写入该行生成瞬间的鼠标坐标，格式如 `[18:34:27] [鼠标 958,415] 日志内容`。移除右上角及悬浮窗中的固定实时坐标标签，坐标随对应事件永久保留，便于事后核对鼠标来回跳动时每一步的位置。

- 修复全局检测在打断工作流并恢复断点后立刻重复触发的问题：触发锁现在跨监控重建保留，新一代监控必须亲自确认图片已经消失，才允许同一模块下次触发。旧监控退出前报告的短暂消失不再解除新监控的锁，避免“点击游戏画面”持续抢占工作流。

- 全局模块启用阶段和检测线程统一通过解析后的模板路径读取模块对象，启用日志显示的持续时间与实际生效值一致，不再出现“启用显示 1000 ms、实际按 100 ms”矛盾。

- 脚本编辑器中的模块引用不允许修改共享模块对象，但可以像其他动作一样双击或点击“编辑”，打开轻量的“编辑模块引用延时”窗口，单独设置当前这一次引用的“执行前延时”和“执行后延时”。模板图片、识别区域、成功动作和附加代码段仍只能在“模块管理…”中修改；保存延时时会保留模块路径、引用标识及其他动作字段。

- “批量加入脚本”新增分类筛选：全部、关卡、关卡封装、切换、全局，按钮直接显示各分类脚本数量，列表同时增加“分类”列。切换分类不会清除其他分类中已经勾选的脚本；“全选 / 全不选 / Ctrl+A”只作用于当前分类，切回“全部”可统一检查和提交所有勾选结果。分类优先读取脚本保存的类别，旧脚本则按当前配置的脚本目录自动判断。

- 脚本编辑“添加动作”新增紧凑的“⇢ 跳转”动作，可选择“脚本开头（从第 1 行开始）”、任意一行目标动作或“脚本结尾（立即结束当前脚本）”。指定行按稳定动作 ID 保存，目标动作调整顺序后仍能正确跳转；目标被删除时会明确报错。跳到脚本结尾时，独立运行会直接结束，工作流会继续下一项。

- 新增/编辑模块对象的“附加代码段 → 添加…”新增固定动作“结束当前脚本，执行工作流下一项”，在“引用模块… → 特殊模块”中也可选择。切换模块的嵌套代码段会直接把结束信号传回外层脚本；全局模块打断工作流时，会先执行到该动作再结束原脚本并进入下一项，不会错误恢复旧断点。

- 模块对象管理新增“批量加入脚本…”：选择一个模块后打开带勾选状态的脚本清单，支持逐项勾选、双击/空格切换、Ctrl+A 全选以及“全选 / 全不选”，确认后把该模块插入所有勾选脚本的第 1 行。脚本来自关卡、关卡封装、切换、全局等当前配置目录；批量加入全局模块时会跳过全局脚本，防止检测模块递归嵌套。

- 普通脚本中的全局模块新增跳转目标“脚本结束（工作流中执行下一项）”。触发后会先完整执行模块主动作和附加代码段，再把当前工作流次数记为完成，立即结束当前脚本并从工作流下一项继续；若当前已经是最后一项，则正常结束整个工作流。该目标使用固定标识，不受脚本动作增删或排序影响。

- “添加识图动作 / 编辑识图动作”的模板栏新增“框选新建…”：隐藏软件窗口后直接拖框，自动把框内画面保存到当前识图图片目录，并把该框记录为这个动作自己的检测区域，无需预先创建模块对象。临时截图不会自动加入模块对象仓库，之后可在“图片采用情况”的未采用列表中按需建立模块。

- 识图动作的“找到后跳转目标动作”新增“直接结束当前脚本，执行工作流下一项”。识别成功后会把当前次数记为完成，立即跳过当前脚本剩余动作以及本次工作流执行中的剩余重复次数，再进入下一条工作流任务；单独运行脚本时则直接结束当前脚本。该目标使用固定控制标识，不受动作增删或排序影响。

- 恢复并重构游戏级强制专注锁定：Windows `BlockInput` 会同时封锁游戏读取的 Raw Input/DirectInput 实体键鼠输入，不再只锁住桌面光标。MacroFlow 的所有 `SendInput` 回放动作统一转发到持有系统输入锁的专用线程，因此普通脚本、相对坐标轨迹、全局检测点击和全局模块代码段在锁定期间仍能正常发送；结束、报错或紧急停止时由同一线程可靠解除锁定。

- 修复开启“强制专注模式”后部分鼠标、触控板或驱动层输入仍能移动光标的问题：MacroFlow 回放产生的键鼠事件现在带有专用标识，专注模式只放行本软件自己的回放输入和实体 F12；普通实体输入以及其他软件或驱动注入的键鼠事件都会被拦截。

- 修复启用“执行前置窗口”后在工作流中没有真正前置的问题：显式前置窗口现在独立于“执行时前置目标”开关生效；当前编辑器选择的前置窗口会作为整个工作流的默认值，步骤脚本保存了自己的前置窗口时仍优先使用脚本设置。工作流切换脚本时会重新按窗口签名解析，避免错误复用上一个脚本的窗口。

- 选择“二次识别模板”后，二次识别自动使用该模板对象登记的框选区域，模块编辑页不再显示或要求单独设置“二次识别区域”；模板对象未登记有效区域时自动按全屏识别。旧模块里单独保存的二次识别区域会被忽略并清理，避免两份区域配置互相冲突。

- 模块对象编辑窗口从创建的第一帧就定位在屏幕顶部，不再先显示在屏幕中间、等内容布局完成后再跳到顶部。

- “新增模块对象 / 编辑模块对象”窗口打开后改为水平居中并贴紧屏幕顶部，不再垂直居中占掉上下空间；较高表单能显示更多底部内容，通常无需先拖动窗口。其他普通弹窗仍保持原来的居中位置。

- 模块对象的“成功后动作”下方新增可选“动作完成后再执行代码段”：先完成点击识别位置、点击自定义位置、成功后继续或二次识别后点击，再依次执行附加代码段，代码段完成后才继续原脚本或从全局模块打断的工作流断点恢复。代码段的“引用模块…”现在也显示特殊模块，可插入“重新执行工作流”；触发该特殊模块时由它接管执行流，从工作流开头重新执行，不会再错误恢复旧断点。旧版互斥选项“成功后执行代码段”会自动迁移为“成功后继续 + 附加代码段”，原动作不丢失。

- 工作流的“全局模块”列表新增适配模块对象的右键菜单：可选择“在当前编辑器打开模块”直接修改该对象，或“在新窗口编辑模块”启动独立 MacroFlow 窗口并自动打开同一对象；旧工作流中的脚本型全局模块仍显示打开脚本选项。模块图片更换后，当前工作流行会同步更新引用路径；对象已移除时会明确提示。

- 修复全局模块连续触发两三次后弹回主界面且工作流不再继续的问题：断点恢复前会一次性停止并清空上一轮监控；全局模块处理中禁止旧监控重新注册或并发触发；暂停期间保持执行界面，模块完成后再从原脚本、原次数、原动作断点继续。日志中的“触发后 触发后”重复文字也已修正。

- 工作流添加全局模块支持批量选择：Ctrl 单独增减选择、Shift 连续范围选择、Ctrl+A 全选；点击“选择”后一次加入全部选中模块。普通脚本中的模块插入仍保持单选，避免误插入。

- 模块对象的“二次识别后点击”新增点击目标：第一次识别位置、第二次识别位置、自定义框选区域。自定义方式点击框选区域中心；二次识别搜索区域与点击区域分别设置。旧模块未保存该字段时仍默认点击第二次识别位置。
- 工作流“添加全局模块”现在直接打开模块对象仓库，并且只显示“全局模块”；不再要求新建检测配置或选择脚本。双击已有行也直接更换模块对象，旧工作流中的脚本型全局模块仍兼容执行。
- 全局模块触发时立即暂停当前脚本，记录工作流步骤、当前重复次数和脚本动作断点；全局模块完整执行后，从同一脚本的同一次重复、被打断动作位置继续，不会跳到下一次，也不会让原脚本在二次识别等待期间继续运行。

- 工作流步骤和全局模块删除后都支持“撤销删除”，并可连续撤销多次。删除全局模块后会自动选中原位置的下一行；删除末行时选中上一行，因此可以连续点击“删除”，不会因重建列表而丢失选择。

- “执行时前置目标”现在只控制执行前是否激活目标窗口；取消勾选不会清空目标绑定，重新勾选即可继续测试。原“解除”按钮改为含义明确的“清除绑定”，只有主动点击它才会删除目标；新选择的目标窗口会立即保存。

- 模块对象管理的拼音排序改为直接点击表格第一列标题切换，不再额外占用两个按钮。标题后的 `↑` 表示拼音升序、`↓` 表示拼音降序；图片清单点击“图片文件”，模块页点击“模板图片 / 名称”或“模板图片”。

- 模块对象管理支持拼音升降序，适用于全部模块、切换模块、全局模块、特殊模块和图片采用情况；模块列表按模块名称拼音排序，图片清单按不含后缀的图片文件名拼音排序。排序使用 `pypinyin` 词库，不按 Unicode 或笔画近似排列。

- “图片采用情况”增加 `全部图片 / 已采用 / 未采用` 三个直接筛选按钮；选择“未采用”时表格只列出尚未建立模块对象的图片，选择“已采用”时只列出已经登记的图片，顶部统计同时显示总数和当前筛选数量。

- “模块对象管理”新增可持久化的**识图文件夹**设置，默认使用程序目录下的 `images\\部分`（打包版即 `dist\\images\\部分`），也可选择其他目录。新增“图片采用情况”页签，递归扫描 PNG/JPG/JPEG/BMP，分别标记“已采用 / 未采用”并显示统计；双击未采用图片可直接创建模块，双击已采用图片可直接编辑对应模块。选择模板图片和截图新建也默认进入该目录。

- 所有下拉选项框禁止通过鼠标滚轮切换当前选项，避免滚动页面时误改设置；点击展开下拉列表后，列表本身仍可正常滚动并点击选择。

- 模块对象名称默认取模板图片路径的最后一个文件名并去掉扩展名，例如 `images\\部分\\勇士挑战确定.png` 默认命名为“勇士挑战确定”。名称会真正保存到模块对象，并用于模块列表和脚本摘要；用户手动填写过的名称不会在重新选图时被覆盖。

- 修复“选择前置窗口”后重启或打开旧脚本看似丢失的问题：选择、启用/停用或解除前置窗口时立即保存最近选择；被动打开未配置的旧脚本不再清空该记录和勾选，并会继承最近保存值。已有脚本明确保存了自己的前置窗口时仍以脚本设置为准。

- 主界面与工作流工具栏重新分组换行，移除会裁掉文字的窄按钮宽度；脚本文件操作、添加动作、插入位置和编辑操作分区显示，在常用窗口尺寸下按钮文字均可完整看到。侧栏的长篇说明改为字段旁的 `?` 悬停提示，减少占用空间。

- 模块对象管理改为**双击普通模块直接编辑**，不再提供右键菜单；特殊模块是软件内置的固定动作，不能新增、编辑或移除。插入模块窗口的说明同步精简，特殊模块不再显示“新建模块”。

- “编辑模块对象”窗口重新整理为“基本信息 / 识别设置 / 成功后动作”三个区块；每个需要解释的字段旁都有可见的 `?`，悬停后显示简短说明，输入框与按钮统一对齐，保存按钮始终可见。

- 特殊模块“重新执行工作流”只对**当前正在运行的工作流**生效：执行到该动作时从工作流第 1 步重新开始；单独运行脚本时没有当前工作流，因此直接跳过，不再把独立脚本自身无限重跑。

- 修复"编辑模块对象"表单**只有分类和模板图片能改、其他字段点了没反应**的问题：各字段的输入控件原来是用 `grid(in_=…)` 放进所在行的网格里（控件本身仍挂在表单根框架下），在可滚动（Canvas 包裹）的表单里这种布局下，控件的窗口没有落在它声明的位置上——真实点击落不进输入框、焦点进不去，所以相似度 / 名称 / 检测间隔 / 延时 / 点击位置等全部无法键入，只有不需要焦点的分类下拉和模板图片按钮能用。现在每个字段控件作为所在行的**真实子控件**创建，布局外观不变，所有输入框点击即可直接键入（如相似度直接输 0.85）。同时：模态表单打开时置前并激活，避免点击被别的窗口截走；表单首次显示时再校正一次滚动区域。

- 脚本编辑页新增**重做**：编辑动作后点"↷ 重做"按钮（或 Ctrl+Y / Ctrl+Shift+Z）恢复撤销前的编辑，撤销 / 重做可来回切换；新的一次编辑会清空重做历史，保存脚本后撤销 / 重做历史一并清空。

- 修复悬停说明框遮挡输入框的问题：字段说明弹出框原来定位在字段名正下方，恰好盖住同一行右侧的输入框（如"相似度"的输入框被说明文字盖住，看起来"没有可以输入的地方"，点上去也被弹窗吞掉）。现在说明框固定弹在**整行的下方**，字段名右侧的输入框始终可见可点（如"相似度"右边直接输入 0.85，"框选区域"右边直接输入坐标或点"框选区域…"按钮框选）。

- "编辑模块对象"表单界面精简 + 悬停说明：每个字段的名称（名称、模板图片、框选区域、相似度、检测间隔、阻塞识别、持续超过、延时、识别成功后执行、点击按钮、点击位置、二次识别等）都有填写的输入控件，鼠标悬停在字段名上会弹出该字段的作用说明（如悬停"延时"显示"识别成功后、执行动作前的等待毫秒数"），不再占用表单空间；冗长的行内说明和窗口顶部大段文字已压缩为简洁表述（"识别成功后动作 B"更名为"识别成功后执行"，"延时 A"更名为"延时"，去掉示例性的 A/B 命名）。

- 模块对象管理窗口支持**撤销移除**：误删模块后点"撤销移除"按钮（或按 Ctrl+Z）即可恢复条目及其全部属性，自动重新选中并写回仓库，其它脚本中的引用立即恢复；没删过东西时按钮禁用。多次移除可连续逐级撤销。

- 修复"编辑模块对象"表单分类下拉选不了的问题（1.82.1 回归）：可滚动表单把每个输入行包在行框架里时，输入控件被错放进表单根网格的同一个单元格，所有字段叠在一起把分类下拉框盖住，点下拉箭头没反应。现在每个输入控件正确放进所属行，分类下拉可正常切换（选"全局模块"显示"持续超过"，选"特殊模块"进入纯动作模式），名称行也恢复对普通模块显示。另：插入模块窗口列表为空通常是运行目录里没有数据文件——把 `template_regions.json` 和 `images/` 等数据文件放在 EXE 旁边（或直接在旧的数据目录里运行 EXE）即可。

- 修复"编辑模块对象"表单下部字段看不见的问题：表单行数多，打包版按真实 DPI 渲染（或屏幕较矮）时，固定高度的窗口会把底部的"延时 A / 动作 B / 点击按钮 / 点击位置 / 二次识别 / 保存按钮"等行挤出窗口，没有滚动条就够不到，看起来"相似度、检测间隔、延时、动作 B、点击按钮等都没有输入的地方"。现在表单包在可滚动区域里：窗口自适应内容高度，屏幕放不下时出现滚动条，滚轮或拖滚动条可到达所有字段；分类 / 动作 B 切换时窗口大小随内容联动。特殊模块（纯动作，只需填名称）仍只显示分类和名称，属正常设计。

- 模块对象仓库恢复 **3 个类别：切换模块 / 全局模块 / 特殊模块**，各司其职：切换模块 = 识图引用（image_match）；全局模块 = 检测型（模板图片 + 区域 + "持续超过"时长，插入"全局检测"引用，即旧版全局模块）；特殊模块 = 纯动作（无图片，如"重新执行工作流"）。1.81 曾把旧全局模块并入特殊分类，现在**检测型特殊条目（有图或未标纯动作）加载时自动迁回"全局模块"**，字段全保留；纯动作特殊条目保持不动。模块对象管理窗口改为 **4 个可点击切换的页签：全部 / 切换 / 全局 / 特殊**（全部页签混排三类，纯动作显示名称、区域为"—"；切换 / 全局页签列模板图片与框选区域；特殊页签列名称与类型）。对象表单分类下拉 = 切换模块 / 全局模块 / 特殊模块：选"全局模块"显示检测型属性（含"持续超过"），选"特殊模块"进入纯动作模式（只填名称）；切换模块行为不变。插入模块窗口页签同步为"切换模块 / 全局模块 / 特殊模块"，全局模块插入 global_detect 引用、特殊模块纯动作插入 restart_workflow。旧脚本里的 `module_category="global"` 与 1.81 的 `module_category="special"` 引用都照常生效。

- 模块对象仓库的类别精简为两类：**切换模块** / **特殊模块**（原"全局模块"类别整体并入"特殊模块"，旧配置自动迁移）。"模板区域"管理窗口改为 **3 个可点击切换的页签：全部 / 切换 / 特殊**——不再有"分类"列；全部页签混排两类（纯动作显示名称、区域为"—"），切换页签列模板图片与框选区域，特殊页签列名称与类型。**特殊模块现在可以入库新建 / 编辑 / 删除**：表单分类下拉只剩"切换模块 / 特殊模块"，选"特殊模块"且未选图片时进入**纯动作**模式（只需填名称，行为固定为「重新执行工作流」，如常驻的"重新执行工作流"）；选图后即为检测型特殊模块（沿用原全局模块的检测属性，运行时行为不变）。插入模块窗口的页签同步改为"切换模块 / 特殊模块"，特殊模块纯动作插入 restart_workflow 动作、检测型特殊模块插入 global_detect 引用。旧脚本里存的 `module_category="global"` 引用照常生效。

- "模板区域"管理升级为**模块对象仓库**：模板分 3 类——**切换模块**（脚本中的识图动作）、**全局模块**（脚本中的全局检测动作）、**特殊模块**（纯动作，无图片）。每个模块对象除了图片和搜索区域，新增行为属性：**阻塞识别**（识别不到就一直等，不往下走）、**识别成功后延时 A 后执行动作 B**。动作 B 有 5 个选项：点击识别区域 / 点击自定义位置 / 直接 continue / **二次识别后点击**（识别到模块 → 延时 A → 对另一个已登记模板识别，可限定区域 → 点击其位置）/ **执行代码段**（代码段里还可以再插入模块，递归嵌套最多 8 层）。

- 脚本编辑页头部新增"**▤ 模块**"按钮：打开 3 分类窗口，把仓库中的任意模块对象插入脚本（切换/全局各一个列表，另有"特殊模块"标签页）；也可以直接在各分类里"新建模块…"，表单保存后自动同步进仓库，下次所有脚本都能引用。**实时引用**：插入脚本的只是引用，运行时从仓库实时读取对象属性——在"模板区域…"里修改对象（区域、阻塞、延时 A、动作 B），所有引用它的脚本立刻生效；旧脚本（内联图片 + 区域）完全兼容，行为不变。

- 新增特殊模块"**重新执行工作流**"：运行工作流时执行到它，整个工作流从第 1 步重跑（各行的剩余执行次数也一并恢复）；从 1.83.0 起，单独运行脚本时会跳过该动作。

- 修复"模板区域"管理器中框选区域 / 截图新建时被本软件窗口遮挡的问题：现在点"新增模块"打开表单后，无论从主窗口还是从识图 / 全局识图对话框进入，框选区域（或截图新建）时整条窗口链——主窗口、上级对话框、管理器窗口、表单——会全部暂时隐藏，屏幕上只剩框选幕布，想框哪里就框哪里；"截图新建…"截出的模板图片也不会再把本软件自己的窗口截进去。框选完成或按 Esc 取消后，所有窗口自动恢复原样（最大化状态也保留）。

- 识图 / 全局识图动作不再需要手动添加模板图片和框选区域：直接引用"模板区域"管理器中已登记的模板即可。动作对话框里"模板"一栏改为下拉列表，从登记表中选择模板，检测区域运行时自动从登记表实时读取（模板 = 模板图片 + 框选区域，在"模板区域…"中统一管理）。修改模板后，所有引用它的动作自动生效，无需逐条重设；备用模板同样支持引用。旧脚本（内联图片 + 区域）完全兼容，照常运行。

- 程序启动时默认申请管理员权限：打包版 EXE 内置 `requireAdministrator` 清单，每次启动都会弹出 UAC 确认后以管理员身份运行。保证全局检测点击、模拟输入等操作在同样以管理员身份运行的目标程序（如游戏）上也能正常生效，避免"日志显示已点击但实际没有点击"的权限问题。

- "模板区域"管理窗口精简为三个按钮：新增模块 / 移除所选模块 / 关闭。"新增模块"打开表单，必须填写模板图片（"选择图片…"登记已有文件，"截图新建…"框选屏幕区域生成新模板图片，截图区域自动作为默认搜索区域）并框选区域后才能保存；修改已有模板不用再找按钮——在列表条目上点右键，菜单里选"编辑模板…"（可更换图片或重新框选区域）或"移除所选模板"。

- 修复"模板区域"管理窗口里按钮全部看不见的问题：此前窗口高度固定为 470，打包后的程序按显示器真实 DPI 渲染时内容实际需求高度更大，底部按钮行（新建模板… / 添加模板… / 编辑模板… / 框选区域… / 移除所选模板 / 关闭）被挤出窗口底部，窗口里只剩列表，看起来"什么按钮都没有"。现在窗口打开时按内容实际需求自动调整大小并重新居中，屏幕放不下时允许手动拉伸，按钮行始终可见可点。

- "模板区域"管理模块支持"新建模板…"：从屏幕上按住左键拖选一块区域，自动截图存为新模板图片（images 目录下的 template_时间戳.png）并立即登记到列表里，截图区域同时作为该模板的默认搜索区域；之后在识图 / 全局识图对话框里选中它时区域自动导入，也可用"框选区域…"另行修改。配合原有的"添加模板…"（登记已有图片文件）、"编辑模板…"（更换图片）、"移除所选模板"，模板的创建、登记、修改、删除全部可以在管理模块里完成。

- 修复"模板区域"管理里"添加模板"后条目丢失的问题：此前添加模板会立即弹出框选区域，取消框选时条目从未保存，关闭窗口后新建就没了；且区域未设置的占位条目（宽高为 0）在重新打开时会被当作无效数据丢弃。现在"添加模板…"选择图片后立即保存新条目（显示"未设置区域"），之后随时可用"框选区域…"设置搜索区域、"编辑模板…"更换图片、"移除所选模板"删除；占位条目在重新打开时也会保留。区域未设置的模板在识图对话框中选中时提示"该模板未设置区域"，不会导入空坐标。

- "模板区域"管理模块支持插入和编辑每个模板："添加模板…"登记新模板图片，"编辑模板…"可把已登记的条目换成另一张模板图片（搜索区域保留、改挂到新图片上），框选区域 / 移除模板照旧。模板图片与搜索区域的对应关系只能在这个管理模块里修改（脚本编辑页头部"模板区域…"打开），识图和全局识图对话框里选中模板只是读取导入，绝不会改动登记。

- "模板区域"管理模块的入口移到脚本编辑页面：脚本编辑页头部新增"模板区域…"按钮，打开统一的模板区域管理窗口，可以查看哪些模板图片已经登记了搜索区域、在同一个窗口中统一框选修改或移除。识图与全局识图共用同一份登记（不区分对话类型），在"识图"或"全局识图"对话框中选中模板图片（包括备用模板）时，登记的搜索区域会自动导入。

- 新增"模板区域"注册表：可以为每个模板图片预先登记一个默认搜索区域（模板区域管理对话框，识图与全局识图的模板图片行上也有"模板区域…"按钮直达）。之后在任何"识图"或"全局识图"对话框中选中该模板图片（包括备用模板），它登记的搜索区域会自动导入——搜索区域自动切换为"自定义区域"并填入框选的坐标，无需每次手动重设。未登记的模板选择时会提示"该模板未设置区域"。注册表保存在项目目录的 template_regions.json，随时可增删改。

- 全局模块触发跳转的日志 / 执行小窗 / 进度文字不再只显示"第几行"：跳转目标现在带具体内容，例如"跳转到第 3 行（键盘：A）执行"、"从第 5 行（延时：500 ms）继续执行脚本"，一眼能看出要跳到哪一步；目标行是识图、文本、打开软件等动作时同样显示对应信息。启用检测时的日志（"触发后跳转到…"）也一并显示目标行的内容。

- 全局模块（普通脚本内嵌全局模块行）的跳转目标改为"行的对象"：添加 / 编辑时不再手输行号，而是从脚本的行列表下拉框里直接选择要跳转到的行（每行显示"第 N 行 · 动作类型 · 内容"）。跳转按动作的唯一标识保存，之后在脚本里插入、删除、移动行都不会失效——触发时自动解析到该行当前所在的位置；目标行被删除时，脚本摘要会明确提示"触发后跳转目标已删除"。旧脚本（只存行号）自动兼容，编辑一次后升级为行对象引用。

- 键盘动作插入支持按键自动检测：点击"检测按键…"按钮后，直接按下要插入的按键即可自动填入按键名（无需手动输入）。检测期间该按键会被拦截不会误触其他窗口，按 Esc 取消；F8 / F9 / F12 为软件快捷键，检测时会自动跳过。数字键 0-9 现在按字符解析（如输入 "0" 表示数字键 0 = VK 0x30），不再被当作十进制虚键码。

- 普通（非全局）脚本新增"内嵌全局模块"动作（◈ 全局）：把全局检测写进普通脚本的任意一行，播放到该行时自动启用全局检测（检测图片在区域内持续出现超过设定时长），触发后停止当前播放、跳转到脚本第 N 行继续执行，播放到脚本末尾后结束（不再继续检测）。编辑对话框里"触发后跳转到脚本第 N 行"决定跳转目标（行号从 1 起）。旧脚本兼容：原来的"全局检测"动作（触发条件）只属于全局脚本，迁移逻辑跳过带跳转行的模块行；带模块行的普通脚本不会被误判为全局脚本（不再强制类别为"全局"，也不再影响全局脚本标记）。

- 脚本编辑器中删除"添加全局检测动作"入口，全局脚本的触发条件改为独立配置区：类别为"全局"的脚本，编辑器上方显示"◈ 触发条件"区块（配置检测图片 / 区域 / 持续时长 / 阈值），下方"要执行的动作（触发后按顺序执行）"是语句体。触发条件满足（图片在区域内持续出现超过设定时长）时依次执行语句体里的所有动作；执行完继续检测，条件仍满足则再次触发。旧脚本自动迁移：原"全局检测"动作自动转为触发条件，语句体只保留其余动作；迁移在打开、保存、被工作流引用时幂等生效。单独 F9 执行全局脚本时保持检测运行，触发后执行语句体，F12 停止；旧脚本（语句体为空）触发时仍点击识别到的位置，行为不变。触发条件里的点击、延时等操作全部移入语句体。

- 全局检测改为多监控同时运行：工作流里的每个全局模块（7 个弹窗 = 7 个脚本）现在各自拥有独立的检测监控，全部同时识别，不再只有最后一个模块生效。全局脚本 = 一个触发条件 + 语句体：条件满足（图片持续出现超过设定时长）时停止当前工作流并记下打断位置，执行该脚本的语句体（点击等步骤），完成后从被打断的那个脚本的下一行继续执行；检测不停止，条件仍满足则再次触发。此前启用多个全局模块时只有最后一个模块的检测真正在运行，其余模块的触发条件永远检测不到。

- 修复开启"强制专注模式"时全局检测点击完全失效（光标不动、触发条件永远无法解除、每 1 秒反复触发直至手动停止）的问题：移除 Windows `BlockInput` 系统级输入锁定。`BlockInput` 会静默丢弃"非调用线程"的 SendInput 注入，而全局检测点击和全局模块步骤在独立线程执行，注入事件全被系统丢弃；现专注模式的实体输入锁定完全由低层钩子承担（钩子拦截实体键鼠、放行任何线程的注入输入），全局检测点击恢复正常。

- 全局检测的"点击后延时"默认值从 0 改为 1000 ms：未显式设置延时的全局模块/全局检测动作，触发点击后默认等待 1 秒再继续执行（恢复工作流），保证点击来得及生效；已显式设置过延时的配置不受影响。

- 修复"在新窗口打开脚本 / 新开窗口"启动第二个实例时报"Failed to start embedded python interpreter!"的问题：启动子进程时清除继承的 _MEIPASS 环境变量（PyInstaller onefile 的 bootloader 会尝试复用父进程的临时解压目录，目录被锁定或清理时启动失败），子进程改为解压自己的全新临时目录。

- 工作流新增结束逻辑：当所有计次脚本（设置了执行次数且未禁用）都已执行完毕（次数用完）后，整个工作流自动结束并停止全局检测，不再循环执行剩余的不计次数脚本；全部为不计次数脚本的工作流不受影响，照常无限循环。

- 工作流"全局模块"操作补齐：双击模块行可直接更换引用的脚本（与工作流步骤一致，弹出文件选择替换），右键菜单支持"⇪ 在新窗口打开脚本"或"✎ 在当前编辑器打开"；有脚本的模块行前显示"⇄ 引用脚本"标识（无脚本的纯检测模块仍显示"◈ 全局检测"）。编辑全局检测配置改为通过工具栏"编辑选中"按钮。

- 修复全局检测"日志显示已点击但实际没有点击"的排查问题：触发点击后新增光标位置核对——SendInput 可能返回成功但事件被系统丢弃（典型：游戏以管理员身份运行而 MacroFlow 不是，或输入被拦截），现在光标与目标位置不一致时会明确提示。点击信息同步写入执行滚动小窗（"全局检测已点击 (x, y)"、"全局检测触发：已点击，继续检测。"）。识别到时日志附带匹配位置坐标（"识别到 xx @ (x, y)"），方便核对点击位置是否对准目标。
- 全局检测未配置"点击位置"时不再什么都不做：改为点击识别到的位置（与识图动作默认行为一致），配置了固定点击位置则仍按固定位置点击。

- 全局检测运行过程不再静默：检测图片文件不存在、识别出错时日志会明确提示（各只提示一次，不刷屏）；识别到图片但未达到“持续时长”时记录“识别到 xx，等待持续超过 xx ms 后触发”，图片中途消失时记录“图片已消失，计时重置”。此前这些情况完全没有日志，导致“加了全局检测没反应”时无法判断是图片没配、图片闪断还是持续时长不够。

- 全局检测（全局模块脚本里的“全局检测”动作）的区域不再局限于目标窗口：与识图动作一致，支持“全屏 / 自定义区域 / 目标窗口”三种模式。旧配置自动兼容：已有自定义区域且未设置模式时按自定义区域，否则按全屏。目标窗口模式下每轮检测都会实时读取当前绑定的窗口位置和尺寸，未绑定窗口时退化为全屏检测。

- 全局模块执行方式重做：运行工作流时，全局模块只并行开启全局检测、不再预先执行模块脚本；检测到模块的图片持续出现后，先停止当前工作流（记住被打断的步骤、重复次数和脚本内位置），点击指定位置并延时，然后把全局模块脚本里的步骤完整执行一遍，最后从被打断的那个脚本的“下一个动作”继续执行（该次重复的剩余动作完成后，后续重复照常从头执行）。全局模块配置对话框的“触发后跳转到指定行”选项已移除，改为始终“先执行模块步骤，再继续原工作流”。全局模块列表现在始终显示每个模块关联的全局脚本名称。触发执行完成后检测不会停止：只要触发条件仍然满足（图片仍持续出现），就会再次触发执行（单独测试脚本与工作流中行为一致）。单独用 F9 执行含“全局检测”动作的脚本时不会“执行完成”结束，而是保持全局检测持续运行：检测到图片超过设定时长就自动点击，没检测到就继续检测，按 F12 停止；触发后只执行点击，不会启动工作流。所有识图动作和全局识图识别到后，都会在识别到的目标区域周围显示一圈细边框颜色提醒（红色，约 1 秒后自动消失；备用模板命中时也会提醒）。修复全局检测触发时引用已移除界面变量的残留问题：此前单独用 F9 运行全局脚本时，图片识别到后点击不会执行（触发回调异常静默中断），现已恢复为检测到图片超过设定时长就自动点击、没检测到就继续检测。

- 识图动作的备用模板新增两个选项：① “备用模板出现后点击它”（默认勾选，不勾选则只检测不点击）；② “备用出现后”选择“回到主模板的检测”（处理完备用后继续等待主模板）或“直接退出识别”（处理完备用后结束本次识图，继续执行后续动作）。旧脚本未设置时保持原有行为：出现即点击、点击后继续等待主模板。

- 录制的“鼠标点击”动作编辑对话框新增“执行前延时 (ms)”参数：双击录制点击行可设置该点击执行前的等待毫秒数（默认 0，保持原节奏不变）。

- 修复录制的“鼠标点击”动作（鼠标按键）双击编辑问题：此前双击录制的点击行只会弹出 JSON 高级编辑，现在直接打开“编辑鼠标点击”对话框，可修改鼠标键、按下/松开状态、执行前延时，坐标保留原值。

- 工作流表格支持右键菜单：在脚本步骤上右键可选择“⇪ 在新窗口打开脚本”或“✎ 在当前编辑器打开”（未保存修改时会提示先保存）。

- 移除“关闭 Clash 代理”动作；修复脚本编辑工具栏按钮过挤导致文字被裁剪的问题（缩小按钮最小宽度，删除 1 个按钮后所有按钮完整显示）。

- 脚本编辑新增“关闭软件”动作（✕ 关闭）：填写或从正在运行的进程里选择进程名（如 clash-verge.exe），执行到该动作时结束对应软件再继续后续动作；默认先发送关闭请求优雅退出，超时（可设置等待时长）后自动强制结束，进程不存在时跳过不报错。

- 脚本编辑新增“打开软件”动作（▶ 软件）：通过“选择…”按钮挑选要启动的软件路径（exe/bat/cmd/lnk），可设置执行前延时和执行后延时；执行到该动作时自动启动所选软件再继续后续动作，路径不存在时执行会报错提示。

- 脚本编辑“插入脚本”改为保存“脚本引用”（⇄ 引用脚本）：只记录被插入脚本的文件位置，不再复制其动作；执行时实时读取该脚本的最新内容，之后修改原脚本，引用处会自动跟着更新。支持双击编辑引用目标及执行前/后延时，并检测循环引用与过深嵌套。

- 工具栏中间新增共享“插入位置”按钮组：“▲ 向上插入 / ▼ 向下插入”（选中高亮）。添加动作和“编辑选中动作”里的“⇥ 插入”都按该位置插入；插入脚本按钮恢复为单个。

- 脚本编辑“添加动作”组新增“插入位置”选择（在上方 / 在下方，默认在下方）：选中动作行后，所有添加动作按所选位置插入；上方模式未选中行时插入到顶部。

- 脚本保存目录新增“关卡封装”类目（第 4 个，默认 `scripts/关卡封装`），类别下拉和保存路由同步支持。

- 修复脚本类别从“全局”改不回“关卡/切换”的问题：切换类别时立即写入脚本设置，只有脚本里确实含“全局检测”动作时才强制锁定为“全局”。

- 修复 T 文本动作编辑：双击文本动作不再弹出 JSON 高级编辑，改为专门的“编辑文本动作”对话框，可直接修改文本内容、字符间隔和执行前延时。

- 脚本编辑“添加鼠标点击”的“执行前延时”默认值改为 1000 ms（已有动作的保存值不受影响）。

- 识图动作新增“一直等待直到出现（不超时）”：勾选后目标图不出现就一直循环检测阻塞，出现后再执行找到后的动作（继续/点击/跳转）；勾选时超时相关设置自动置灰，F12 仍可随时停止。

- 修复工作流显示不刷新的问题：切到“工作流”页时自动重建列表，保存脚本后也会刷新工作流里的脚本名、缺失提示和全局模块摘要，脚本改完外面显示立即同步。

- 全局检测支持“触发后不跳转”：勾选“触发后跳转到指定行”时按原逻辑跳转并给当前行次数 +1；取消勾选后，触发时只点击+延时，不跳转、不改次数，工作流继续运行。

- 全局模块列表改为完整摘要显示：检测图片、检测区域、持续时长、跳转目标行都显示出来；脚本型全局模块会读取脚本里的全局检测配置来展示，编辑时也会自动带回完整配置。

- 脚本保存目录改为 3 个类目：关卡、切换、全局，各自独立文件夹（默认 `scripts/关卡`、`scripts/切换`、`scripts_global`）。脚本编辑页名称旁新增“类别”下拉（关卡/切换/全局），保存时按类别路由到对应目录；全局类（或含全局检测动作）的脚本固定进全局目录。

- 脚本编辑页新增“目录设置…”：可分别设置各脚本类目的保存文件夹（支持相对/绝对路径）；打开、插入脚本、选择已有全局脚本都会按对应目录定位。

- 底部状态栏（“就绪”/执行状态、坐标缩放信息）移到窗口最底部并横跨整个窗口，无论窗口多大或多小都始终显示。

- 工作流“脚本文件”、全局模块列和动作“参数”列按内容自动加宽，长文件名（如“返回大厅页面超时.json”）在默认窗口下也能完整显示。

- 工作流页拆成两个独立小框框：上方“全局模块”（独立编号，从 1 开始），下方“工作流”（独立编号，从 1 开始）。全局模块不再混入工作流行；执行时先启用全局模块，再执行工作流行；触发后跳转目标指向工作流面板中的行对象。

- 触发后跳转目标改为绑定“行对象”（每个工作流行都有稳定 ID），跳转目标选择器显示“第 N 行 · 名称”，在目标前插入或移动行后跳转自动跟随，旧的行号跳转会在载入时自动迁移。
- 全局脚本带“◈ 全局脚本”标识（自动检测是否含“全局检测”动作）；“插入已有全局脚本”会列出所有带标识的全局脚本。

- 软件窗口横向加宽（默认 1600×900），工具栏按钮完整显示。
- 工作流新增独立的“全局模块”步骤（“添加全局模块”按钮）：插入后可配置全局检测（检测图片、相似度、检测间隔、检测区域、持续时长、点击位置、点击后延时）以及“触发后跳转到工作流第 N 行”；执行到该步骤即启用全局检测，触发后点击→延时→跳转到指定行重新执行，并把当前执行行的剩余次数 +1（不计次数的行不处理）。

- 全局检测改为脚本动作“全局检测”（添加动作工具栏可插入，也可双击编辑）：把包含该动作的脚本作为工作流中的全局模块执行，即可启用全局检测；检测到指定图片持续出现超过设定时长后，点击指定位置、延时并重新执行工作流。左侧栏的全局检测设置区已移除。
- 浮动提醒位置等下拉框不再随滚轮改变，脚本重复次数等数值项改为“修改/保存”按钮：默认锁定，点击“修改”后才能编辑（包括滚轮）。

- 修复“添加动作”工具栏：7 个按钮总宽度超出可用空间，导致最后的“识图”按钮文字被横向截断；收窄按钮间距后全部完整显示。

- 脚本编辑页新增“新开窗口”按钮：点击后打开第二个 MacroFlow 实例，新窗口从新建脚本开始。

- 修复左侧栏布局：加宽侧栏并重排“全局检测”设置区，检测图片的“选择/框选截图”独立成行，相似度与检测间隔分两行，所有按钮文字不再被遮挡。

- 全局检测的检测图片和检测区域都改为只读，只能通过交互设置：检测图片可用“框选截图…”在屏幕上拖拽截取模板，也可“选择…”已有图片；检测区域只能“框选…”；点击位置只能“点击屏幕选取…”，全部不允许手动输入坐标或路径。

- 所有毫秒类设置（桌面轨迹间隔、全局检测的检测间隔/持续超过/点击后延时）增加“修改/保存”按钮：默认锁定，不能输入、不能点箭头、滚轮也不会改数值；点“修改”解锁编辑，点“保存”锁定并保存。

- 全局检测的“检测区域”改为按住鼠标左键从左上角拖到右下角框选（与识图动作的框选一致），松开即写入 `x,y,w,h`，不再使用两点点击方式。

- 全局检测改为“区域识图 + 持续时长”：可在指定区域（x,y,w,h，留空为全屏）检测图片，图片在区域内持续出现超过设定时长（默认 1000 ms）后才触发点击并重新执行工作流，避免短暂闪现误触发。

- 新增“全局检测”（全局设置，不随工作流保存）：实时检测指定图片，一旦出现就点击指定位置，延时后自动重新执行工作流；可在左侧“全局检测”区域配置检测图片、相似度、检测间隔、点击位置（屏幕点选）和点击后延时。

- 识图动作新增“执行后延时 (ms)”：整个识图动作执行完成后（无论找到后点击/跳转，还是超时后继续/跳转）都会先等待设定的延时，再进入下一条动作或跳转目标。

- 识图动作新增“超时后等待 (ms)”：识别超时/失败后先等待设定的延时，再继续、跳转或停止；与已有的“识别成功后等待”配合，成功和失败路径都可以控制延时。

- 脚本为空白时，“编辑选中动作”里的“插入脚本”现在也可以直接插入，插入到第 1 行；有动作但未选中行时仍会提示先选择插入位置。

- 工作流中“不计次数”的行被选中时，高亮改为对应的绿色主题，与禁用行（橙色）和普通行（蓝色）区分。

- 脚本编辑的“点击”和“鼠标移动”改为屏幕点选记录坐标，不再手填：点击“点击屏幕选取…”后进入全屏幕布，移动光标到目标位置点击即可记录；移动的转向（相对）模式可用“两点测量…”先点起点、再点终点，自动计算 ΔX/ΔY。

- 识图动作新增“找到后跳转”：识别成功后也可跳到指定的动作行，跳转与超时跳转一样跟随目标动作的新位置；旧数字跳转会在载入时自动迁移。
- 工作流新增“不计次数”：勾选后每次执行到这一行都会执行一次，不扣减次数、不会被跳过；双击“执行次数”单元格或“统一设置参数”都可设置。
- 修复空脚本时插入识图动作没有“确定”键的问题（跳转目标为空时对话框不再构建失败）。

- 识图超时跳转改为引用稳定的目标动作对象，不再保存固定行号；在目标前插入动作或移动行后，跳转会自动跟随目标的新位置。旧数字跳转会在编辑器载入时自动迁移。

- 脚本编辑新增“从此运行”：从当前选中动作行开始执行到末尾；F9 仍从第一行开始，识图超时跳转继续使用完整脚本行号。

- 脚本编辑删除动作后自动保持选中：优先选中顶到当前位置的下一行；删除最后一行时选中新最后一行。

- 识图动作默认等待超时改为 3000 ms、超时后继续、执行前延时 1000 ms；新增“超时跳到动作行”，可从指定的脚本行继续执行。

- 提前预热执行提示音和系统托盘资源，避免第一次按 F9 时才初始化造成短暂卡顿；主窗口显示时托盘图标仍保持隐藏。

- 修复双击启动后进程存在但主界面不可见：启动完成后显式恢复并显示主窗口，同时不改变窗口尺寸。

- 脚本编辑工具栏重新排版为“添加动作”和“编辑选中动作”两个等宽分组；统一按钮尺寸、间距和图标，识图与删除使用清晰的强调色。

- 浮动提醒改为单实例固定位置：屏幕上最多显示一个提醒框，新提醒只更新文字并重新计算消失时间，不再叠加多个弹窗。

- 新增“浮动提醒”：自定义文字，默认显示 3 秒且不暂停脚本、不抢占游戏焦点。

- 添加/编辑识图动作时，自定义区域新增“框选…”：框选期间隐藏 MacroFlow 主窗口和识图窗口，只显示全屏选区层。
- 按住鼠标左键从左上向右下拖动，松开后自动写入 `x,y,w,h` 并切换为 custom；按 Esc 可取消并恢复软件窗口。

- 工作流执行次数现在允许设置为 0；成功完成后可从 1 正常减到 0，不再自动禁用该行。
- 启用但次数为 0 的任务显示“○ 次数用完”，下次执行自动跳过；单行编辑和统一设置都可填写 0。

- 工作流执行次数改为剩余次数：某行脚本每成功完成一次，该行执行次数立即减 1，并自动保存工作流。
- 执行失败或按 F12 中途停止的当次不扣；剩余次数减到 0 时自动禁用该行，后续执行直接跳过。

- 脚本编辑新增“撤销”，支持撤销添加、编辑、删除、移动及向下复制，也可在动作列表中按 `Ctrl+Z`。
- 脚本成功保存后会立即清空撤销记录并禁用按钮；保存后的新修改会建立新的撤销记录，最多保留 100 步。

- 脚本编辑新增“向下复制”：支持一行或连续多行，按原顺序复制到所选区域最后一行之后，并自动选中新复制的动作。
- 不连续选择不会复制，避免动作顺序产生歧义。

- 选中禁用任务时改用橙棕色高亮和浅橙色文字；选择正常任务时仍使用蓝色高亮。

- 禁用任务改用橙黄色文字和深棕色整行背景，并显示“● 已禁用”，与正常任务及红色的文件缺失错误明显区分。

- 工作流每一行新增启用状态；选中任务后点击“启用/禁用”，或双击状态单元格即可切换。
- 禁用行会变灰，完整运行和从选中行运行时都会自动跳过，并在执行小窗与日志中显示跳过原因。

- 修复工作流执行偶发弹出“[WinError 0] 操作成功完成”：模拟输入失败时自动重试，并对普通键鼠事件使用兼容发送方式。
- 工作流新增“从选中行运行”，先单击选择任务，即可从该行开始继续执行后续任务，进度仍显示其在完整工作流中的行号。

- “统一设置参数”中的三个参数改为独立勾选；只统一勾选项，未勾选项保留各任务原值。

- 工作流新增“统一设置参数”，可一次把执行次数、开始前等待和重复间隔应用到全部任务。
- 工作流单元格改为单击只选中，双击才修改对应值，避免查看或拖动任务时误弹编辑框。

- 修复执行前置窗口被意外改变大小：普通、最大化和全屏窗口只置于前台，不再执行“恢复窗口”；仅窗口已最小化时才恢复。

- “执行前置窗口”改为独立设置，可与录制/识别使用的目标窗口不同；未单独设置时默认跟随目标窗口。
- 单独选择的前置窗口会保存稳定的标题、窗口类和程序路径，F9与工作流共用；窗口未打开时会提示，不会错误前置其他窗口。

- 执行设置新增“执行前自动前置目标窗口”开关并保存选择，默认开启；关闭后F9和工作流不会抢占前台。
- 关闭自动前置后，如果游戏相对转向开始时目标不在前台，会明确停止并提示，避免操作误发到其他窗口。

- 开始实时读取坐标后临时隐藏主窗口，只显示一个置顶的无边框坐标小窗；小窗实时显示 X/Y 并可直接停止，停止后恢复主窗口。

- 执行设置新增“强制专注模式”开关并持久保存，默认关闭；关闭时仍自动切换英文输入法，但不调用 `BlockInput`。
- 强制专注会阻止独立32位大漠桥接进程发送转向，因此界面明确提示两者冲突；需要大漠转向时请关闭强制专注。
- 执行悬浮窗会分别显示“普通执行中”或“强制专注执行中”。

- 精简侧边栏说明：明确相对转向不缩放，以及 F12 / Ctrl+Alt+Del 的专注模式解除方式。
- 底部状态栏不再显示数据目录，改为显示“脚本基准分辨率 → 当前分辨率”，并标注 1:1 或自动缩放。

- 实时坐标读取不再忽略 MacroFlow 自身区域；开启后鼠标在屏幕任意位置移动都会持续刷新，第二次点击按钮停止。

- “读取当前坐标”改为实时开关：第一次点击后持续显示鼠标位置，再次点击停止并保留最后一个外部窗口坐标，不会被按钮位置覆盖。
- 旧脚本和手动新建脚本缺少分辨率记录时，统一按 1920×1080 作为默认录制分辨率；新录制仍保存实际屏幕尺寸。

- 目标窗口选择器不再显示 MacroFlow 自己的主窗口或弹窗，拖动十字经过自身窗口时也不会重复拼接标题或卡顿，并修复右上角说明乱码。
- 侧边栏新增“读取当前坐标”，点击后显示光标坐标以及当前屏幕分辨率，并在运行日志中记录完整坐标空间。
- 脚本保存录制电脑的虚拟屏幕尺寸；换到不同分辨率电脑执行时，自动等比例缩放桌面绝对移动、点击坐标和自定义识图区域。游戏相对转向轨迹保持原值。

- 工作流每一行新增“重复间隔”，默认 1000 ms，只在同一脚本相邻两次执行之间等待。
- 添加脚本时直接使用默认值，不再连续弹出整行设置；点击脚本、执行次数、开始前等待或重复间隔单元格时，只修改对应单元格。

- 执行悬浮窗固定显示“F12 无响应时按 Ctrl + Alt + Del”的系统级紧急恢复提示，避免用户忘记备用解除方式。

- 专注模式升级为 Windows `BlockInput` 系统级强制锁定，可阻止游戏 Raw Input 产生的实体鼠标转向。
- F12 由锁定前启动的底层监听保留为紧急停止键；脚本完成、异常或停止时都会在播放线程中恢复输入。
- 系统级锁定失败时会取消执行并提示尝试以管理员身份运行，避免误以为专注模式已经生效。

- 自动保存并在下次启动时恢复上次编辑的工作流内容。
- 工作流中找不到脚本文件的行会标红提示，执行时记录原因并跳过该行。

- F9 和工作流执行前自动切换英语输入法；是否锁定实体键鼠由“强制专注模式”开关决定，F12始终可以停止。
- 执行悬浮窗显示工作流脚本进度、脚本总执行次数和当前重复次数。

- 每次开始录制前，自动把目标窗口强制切换为英语（美国）输入法，并关闭中文输入状态。

- F8 新录制会自动与已打开脚本分离，保存时创建不重名的新文件。
- 工作流只显示脚本名称，支持鼠标上下拖动步骤，并提供日期和时间选择器。
- 主窗口、对话框和录制悬浮窗使用统一的深色标题栏；悬浮窗按钮完整显示。

脚本动作列表使用紧凑的“图标 + 动作名称”列，参数列保留更多显示空间。

录制悬浮窗采用紧凑布局，并实时显示鼠标目标坐标、点击坐标、滚轮位置、按键以及桌面/游戏转向模式切换。F8 启动录制不会再对游戏窗口执行“恢复窗口”，避免全屏游戏被改成小窗口。

一个面向 Windows 的本地键鼠录制、脚本编辑、识图和工作流执行工具。应用不联网，脚本均保存在 EXE 附近的 JSON 文件中。
